# 第二次リファクタリング計画

- 起票: 2026-07-05
- 前回: [RefactoringLog2026-07-02.md](../Done/RefactoringLog2026-07-02.md)(第一次、P0〜P2 完了)

## 背景と現状

第一次リファクタリングで `src/client/main.ts` は約 3000 行まで減ったが、その後の新機能
(ポーズ編集・CIGPose・複数人ポーズ・ペイントツール・LLM プロンプト改善・エッジポップアウト等)が
すべて main.ts に追加され、**6012 行**まで再膨張した。`styles.css` も **3791 行**ある。

### 現在の main.ts の内訳(概算)

| ドメイン | 行数目安 | 主な関数 |
| --- | --- | --- |
| イベント配線ハブ | ~860 | `bindEvents`(658 行)+ `handleAction`(202 行) |
| ポーズ編集 | ~1000 | joint ドラッグ / エッジ選択 / マーキー / FK 回転 / Undo / pose worker 連携(L4003〜5023) |
| WebSAM / スマート選択 | ~550 | worker lifecycle / decode / 候補適用(L3437〜4002) |
| マスク編集 orchestration | ~900 | canvas 同期 / ブラシカーソル / ズーム&パン / 島除去 / 反転 |
| ペイントツール | ~400 | ストローク / undo / スポイト / 保存(L5277〜5656) |
| 生成・Round lifecycle | ~700 | `generateRound` / `pollCollectRound` / `interruptRound` / `deleteRoundTree` 等 |
| Project / Template / 設定 / LLM | ~500 | CRUD / import / export / ComfyUI・LLM 接続 |
| draft 永続化(localStorage) | ~350 | generation / inpaint / pose / paint drafts |
| render + フォーム値 getter 群 | ~450 | `render` / `current*Value` ×11 / `fillGenerationFormFromAsset` 等 |

### 再膨張の構造的な原因

1. 第一次は「pure helper の抽出」が中心で、**state に触る orchestration は main.ts に残す**方針だった。
2. その結果 `state` / `render()` / イベント配線に触れられるのが main.ts だけになり、
   **新機能の stateful な部分は main.ts に書くしかない構造**になっている。
3. `bindEvents` / `handleAction` が全機能の中央ハブで、機能追加のたびに必ず肥大化する。
4. ファイルサイズを機械的に見張るガードがない。

つまり「また減らす」だけでは同じことが再発する。今回は **state と配線の構造を変える**。

## 方針(アグレッシブ許容)

1. **state の分離と依存の逆転**: `state` オブジェクト・型を `src/client/appState.ts` に移し、
   各 controller モジュールが `import { state } from "./appState"` する。
   再描画は `requestRender()`(callback 登録式)を appState 側に置き、循環 import を断つ。
2. **ドメインごとの controller 分割**: 下表のモジュールへ移動。関数本文は verbatim 移動を基本とし、
   挙動変更を混ぜない。
3. **action registry 化**: `handleAction` の巨大 switch を `Map<string, (id, target) => void>` にし、
   各 controller が自分の action を `registerActions({...})` で登録する。
   `bindEvents` も各 controller の `bindXxxEvents(app)` に分割する。
   → **以後の新機能は main.ts に 1 行も足さずに追加できる**ことがゴール。

### フェーズ計画

| フェーズ | 内容 | 移動先 | 削減目安 |
| --- | --- | --- | --- |
| A | 基盤: `state`・`requestRender`・action/イベント registry | `appState.ts` / `actionRegistry.ts` | (増える: +200) |
| B | ポーズ編集一式 + pose worker 連携 | `poseEditorController.ts` | −1000 |
| C | WebSAM / スマート選択 | `webSamController.ts` | −550 |
| D | マスク編集 orchestration(canvas 同期・ズーム・島除去等) | `maskEditorController.ts` | −900 |
| E | ペイントツール | `paintEditorController.ts` | −400 |
| F | 生成・Round lifecycle・auto collect polling | `generationController.ts` | −700 |
| G | Project / Template / 設定 / LLM | `projectController.ts` / `settingsController.ts` | −500 |
| H | draft 永続化(generation/inpaint/pose/paint + localStorage) | `draftStore.ts` | −350 |
| I | 残る `bindEvents` 分解・ショートカット | `shortcuts.ts` ほか各 controller | −600 |

**目標: main.ts ≤ 600 行**(boot・composition root・render 呼び出しのみ)。

各フェーズ共通の完了条件:

- `npm run typecheck` 0 エラー / `npm test` 全件パス / `npm run build` / `npm run check` 成功
- ブラウザ smoke(テスト DB・1680x920)で該当機能を実操作
- 独立コミット。作業は git worktree のブランチで行い、完了後 main へマージ
- API / DB / 生成挙動 / HTML 構造 / `data-action` 名 / 文言は変えない

### 再膨張防止(規約のみ、機械的ガードは設けない)

- `AGENTS.md` に「新機能の stateful コードは controller モジュールへ。main.ts への関数追加は禁止
  (composition のみ)」を明記する。
- 行数バジェットの check スクリプトは検討したが不採用(行数は代理指標にすぎず、
  機械的な逃がしを誘発する)。構造(action registry)側で main.ts を触る理由をなくすことを本命とする。

## CSS 計画(Chrome 150 前提、フェーズ J)

`styles.css`(3791 行)は prefix こそほぼ無いが、Chrome 150 で使える機能で大きく整理できる:

1. **CSS Nesting**: 長い子孫セレクタの繰り返し(`.mask-toolbar …` 系など)をネストにまとめる。
   行数と selector 重複が大きく減る。
2. **`color-mix()` によるトークン派生**: `rgba(255,255,255,…)` が 51 箇所、
   accent 系 `rgba(226,173,129,…)` の生値が複数ある。
   `color-mix(in srgb, var(--ink) 9%, transparent)` / `color-mix(in srgb, var(--accent) 42%, transparent)`
   の形に置換し、`--accent-soft/-line/-fill` も `:root` で color-mix 定義にする。
   → accent 変更が 1 箇所で全体へ波及する。
3. **CSS Anchor Positioning**: `.iteration-edge-popout` の位置計算
   (`main.ts` の `positionIterationEdgePopout` + mouseover ハンドラ、fixed 配置ハック)を
   `anchor-name` / `position-anchor` / `position-area` / `position-try-fallbacks` に置換し、**JS を削除**。
   「下→上フォールバック・端クランプ」は position-try がネイティブでやってくれる。
4. **Popover API**: action ドロップダウン(`closeOpenActionDropdowns` による手動 light-dismiss)を
   `popover="auto"` + anchor positioning に置換し、JS の外側クリック処理を削除。
5. **`@scope`**: モーダル / パネル単位のスコープ化で衝突回避の長いセレクタを短縮。
6. **`interpolate-size: allow-keywords`**: サイドバー折りたたみ等の height:auto 遷移を CSS だけで。
7. **ファイル分割**: `tokens.css` / `base.css` / `components/*.css` に分け、esbuild(`scripts/build.mjs`)で
   bundle するか `@import`(ローカル配信なので許容)。
8. **フォントの自己ホスト**: Google Fonts の `@import` はオフライン時にフォールバックフォントになる。
   ローカルアプリなので woff2 を `public/` に置いて `@font-face` 化する(UX 改善 #8 と同件)。

CSS 変更は視覚回帰が起きやすいので、主要画面(Home / Project / 画像詳細 / マスク編集 / ポーズ編集)の
before/after スクリーンショット比較をフェーズ内で行う。

## UX 改善候補(別フェーズ、優先度順)

1. **`render()` の全画面 innerHTML 再構築をやめる**(大工事・最優先候補)。
   現状は毎回 `app.innerHTML` を組み直すため、スクロール保存/復元・rAF 二重復元・
   クリック選択の遅延タイマーなど対症療法が積み重なっている。領域別レンダラ
   (ヘッダー / ツリー / グリッド / モーダル)への分割 + 差分更新にすれば、
   フォーカス消失・画像ちらつき・スクロールジャンプが根本から消える。
   ※ main.ts 分割(A〜I)完了後の独立プロジェクトとして実施。
2. **シングルクリックの人工遅延の解消**: asset card / iteration dot は dblclick と区別するため
   タイマーで選択を遅延しており、クリック反応が鈍い。ダブルクリック機能を別 UI
   (ボタン / 修飾キー)に移して即時選択にする。
3. **Round 削除(`deleteRoundTree`)に確認がない**: Project / Template 削除は confirm があるのに、
   ツリーごと消す Round 削除はダブルクリック→プレビュー経由のみ。confirm か Undo(遅延実行 +
   トーストで取り消し)を付ける。
4. **トーストの改善**: `state.message` は単一文字列なので連続操作で上書きされ、
   info と error の区別もない。種類別スタイル + スタック表示、エラーは自動クリアしない。
5. **生成進捗の可視化**: サーバは ComfyUI WebSocket で進捗を見ているのに、UI は 3 秒 polling の
   枚数増加だけ。round dot / 生成ボタンに進捗 %(または現在 step)を出す。
6. **キーボードショートカットのヘルプ**: `?` で一覧オーバーレイ。マスク / ポーズ編集の
   ショートカットが増えており発見性が低い。
7. **エッジポップアウトの hover 依存**: タッチ・キーボードで出せない。anchor positioning 化
   (CSS #3)と同時に focus / クリックでも表示できるようにする。
8. **フォント自己ホスト**(CSS #8 と同件): オフラインでも見た目が変わらないように。

## テスト手法

### 自動テスト

- `npm run typecheck`(strict, 0 エラー維持)
- `npm test`(現在 281+ 件。リファクタリングでは件数を減らさない)
- `npm run build` / `npm run check`
- テスト DB 必須: `GURUGURU_TEST_DB=1`、データは外部 `GURUGURU_TEST_DATA_DIR`

### テスト用 ComfyUI(ダウンロード済み Desktop 版の流用)

生成パイプライン(workflow patch・ControlNet・unified switch)の実機検証は、
**本番 ComfyUI(8188)には触らず**、DL 済み Desktop 版 ComfyUI のコードと Python 環境を流用した
分離インスタンス(port 8288)で行う。実績: 2026-07-03 の UnifiedSwitch ControlNet 検証。

- コード: `$env:LOCALAPPDATA\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI`(v0.20.1)
- Python: 同ディレクトリの `standalone-env\python.exe`(Python 3.13 / torch 2.10 cu130)
- 共有モデル定義: `%APPDATA%\Comfy Desktop\shared_model_paths.yaml`

手順:

```powershell
# 1. リポジトリ外にテスト用 base ディレクトリを用意(custom_nodes が無いと起動時 FileNotFoundError)
$base = "$env:TEMP\comfy-test-8288"
New-Item -ItemType Directory -Force "$base\custom_nodes", "$base\input", "$base\output", "$base\temp"
# unified switch 検証では未使用枝の LoadImage にも実在ファイルが必要 → input にダミー画像を置く

# 2. 起動(モデルは Desktop 版と共有)
$comfy = "$env:LOCALAPPDATA\Comfy-Desktop\ComfyUI-Installs\ComfyUI"
& "$comfy\standalone-env\python.exe" "$comfy\ComfyUI\main.py" `
  --port 8288 --base-directory $base `
  --extra-model-paths-config "$env:APPDATA\Comfy Desktop\shared_model_paths.yaml"

# 3. 疎通確認(必ず --max-time 付き)
curl --max-time 5 http://127.0.0.1:8288/system_stats
```

GURUGURU 側はテストインスタンス(`PORT` ≠ 5177・`GURUGURU_TEST_DB=1`・外部
`GURUGURU_TEST_DATA_DIR`)を起動し、設定画面の ComfyUI URL に `http://127.0.0.1:8288` を入れて接続する。
GPU 負荷を抑えるため検証は低負荷パラメータ(例: 512x288 / 6 steps)で、
txt2img → img2img → inpaint → pose 添付の順に 1 枚ずつ生成して確認する。

### ブラウザ検証

- テスト DB インスタンスに対して 1680x920 のワイド viewport で smoke(サイドバー・モーダル崩れ確認)。
- 検証後は viewport override を戻す(既存規約)。

## 変えないこと

- API ルート・status code・error message・response shape
- DB スキーマ・保存形式(`request_json` の dataURL null 化規約など)
- 生成 workflow patch の挙動(roleMap 推論・sanitize・ControlNet 添付規約)
- `data-action` 名・HTML 構造・文言(フェーズ A〜I の間。UX フェーズで変える場合は個別に設計)

## 実施記録

(フェーズ完了ごとにここへ追記する)

### フェーズ A(2026-07-05 完了)

- ブランチ `refactor/phase-a-foundation`(worktree)→ main へマージ(b3a47be / cf05f40)。
- 新設:
  - `src/client/appState.ts`(179 行): `state` 本体と `AppState` 型、`GenerationDraft` 型群、
    message 自動クリア(getter/setter)、サイドバー折りたたみの localStorage 永続化、
    `requestRender()` / `setRenderCallback()`(callback 登録式。main.ts が boot 時に `render` を登録)。
  - `src/client/actionRegistry.ts`(39 行): `registerActions` / `actionHandlerFor`(重複登録は throw)と
    `registerEventBinder` / `bindRegisteredEvents`。
- main.ts 配線: `handleAction` は registry を先に参照し、未登録 action のみ従来の if/else チェーンへ
  フォールバック。`bindEvents` 末尾で `bindRegisteredEvents(app)` を呼ぶ。関数本文の挙動変更なし。
- AGENTS.md に「Client Architecture」節を追加(stateful コードは controller へ、main.ts への関数追加禁止)。
- main.ts: 6012 → 5875 行(計画では +200 見込みだったが、state 移動分で純減)。
- 検証: typecheck 0 エラー / test 319 件全パス / build / check 成功。
  ブラウザ smoke(テスト DB・1680x920): プロジェクト作成→詳細遷移、サイドバー折りたたみ
  (localStorage 反映)、エラーメッセージ表示と dismiss、ホーム復帰を実操作で確認。コンソールエラーなし。

### フェーズ B(2026-07-05 完了)

- ブランチ `refactor/phase-b-pose-controller`(worktree)→ main へマージ(375edab)。
- 新設:
  - `src/client/poseEditorController.ts`(1281 行): pose worker 統合(MediaPipe/CIGPose)・
    joint ドラッグ・エッジ選択・マーキー・FK 回転・Undo・pose draft 更新を verbatim 移動。
    `pose-load-model` / `pose-detect` / `pose-reset` は `registerActions` で登録(main.ts の
    if/else から削除)。keydown / pointerdown / move / up / cancel のポーズ分岐は
    `handlePoseEditor*(event): boolean` として公開し、main.ts のハンドラが同じ位置で呼ぶ
    (分岐の優先順位・return 挙動は従来と同一)。
  - `src/client/draftStore.ts`(45 行): `persistProjectDraft` / `restoreProjectDraft` /
    `draftStorageKey` を先行分離(controller → main.ts 依存を断つため。フェーズ H の一部前倒し)。
  - `src/client/clientUtils.ts`(23 行): `clampNumber` / `imageToRawData`
    (pose と mask/websam の共有ヘルパー)。
- main.ts: 5875 → 4608 行(−1267)。`selectedPoseEdges` 等の直接参照は
  `getSelectedPoseEdges()` / `clearSelectedPoseEdges()` / `closePoseEditorSession()` に置換。
- 検証: typecheck 0 エラー / test 319 件全パス / build / check 成功。
  ブラウザ smoke(テスト DB・1680x920): テンプレート登録→ソース画像アップロード→画像詳細→
  マスク編集→ポーズタブで、MediaPipe モデルの実ロード(Ready/GPU 初期化)、ポーズ検出の
  worker 往復(人物なし画像で適切なエラーメッセージ)、pose フィールド更新の localStorage 永続化、
  registry 経由の pose action dispatch、Ctrl+Z、モーダル閉時のセッション破棄を実操作で確認。
  コンソールエラーなし。

### フェーズ C(2026-07-05 完了)

- ブランチ `refactor/phase-c-websam-controller`(worktree)→ main へマージ(cf829bb)。
- 新設:
  - `src/client/webSamController.ts`(702 行): WebSAM worker lifecycle・モデルロード・
    encode/decode・候補選択/適用・point/box prompt・smart mask 設定を verbatim 移動。
    `websam-*` / `set-smart-mask-provider` は `registerActions` で登録、box prompt の
    pointer 分岐は `handleWebSamPointer*(event): boolean` として公開。
  - `src/client/maskLayerStore.ts`(41 行): `maskLayerCache` / `ensureMaskLayerSet` /
    `syncMaskLayerSetFromDraft` / `getOrCreateMaskLayerSet`(websam・マスク編集の共有層)。
- `draftStore.ts` に `inpaintDraftForAsset` / `setInpaintDraft` / `ensureInpaintDraft` を移動。
- main.ts: 4608 → 3901 行(−707)。
- 注意点: フェーズ B の切り出しで `MASK_WHEEL_ZOOM_IDLE_MS` を範囲誤りで消しかけたが
  typecheck で検出し復元(削除範囲は境界行の内容 assert で防護する運用にした)。
- 検証: typecheck 0 エラー / test 319 件全パス / build / check 成功。
  ブラウザ smoke(テスト DB・1680x920): SlimSAM-77 の実ダウンロード(13MB)→ Ready、
  point prompt 追加(FG/BG 1/0)、decode で候補 3 件生成、候補選択→ apply-mask-editor 適用、
  点クリア、モーダル閉を実操作で確認。コンソールエラーなし。

### フェーズ D(2026-07-05 完了)

- ブランチ `refactor/phase-d-mask-editor-controller`(worktree)→ main へマージ(da24678)。
- 新設:
  - `src/client/maskEditorController.ts`(1059 行): マスク canvas 同期・ブラシカーソル
    (querySelector キャッシュ込み)・ストローク描画(rAF バッチ + dirty rect)・
    brush prompt サンプリング・ツールバードラッグ・パネルリサイズ・画像パン・wheel ズーム
    (idle 永続化)・反転 / 島除去 / クリア・タブ切替・inpaint フィールド更新・
    `effectiveMaskDataUrl` を verbatim 移動。mask 系 action 10 件
    (`toggle-mask-editor` / `apply-mask-editor` / `mask-tool` / `clear-mask` / `invert-mask` /
    `remove-mask-islands` / `set-mask-panel-tab` / `clear-inpaint` / toolbar min/restore)を
    `registerActions` で登録。pointer 分岐は `handleMaskEditorPointer*` /
    `handleMaskStrokePointer*`(boolean 返し)として公開し、main.ts のハンドラが同じ優先順位で呼ぶ。
    モーダル閉時の一括破棄は `closeMaskEditorSession()` に集約。
  - `src/client/assetLookup.ts`(14 行): `findAsset` / `assetDimension` を共有化
    (mask controller と main.ts 双方が利用)。
- フェーズ C 以来 main.ts に残っていた未使用 import(websam 型・models・constants 等)も整理。
- main.ts: 3901 → 2913 行(−988)。
- 検証: typecheck 0 エラー / test 319 件全パス / build / check 成功。
  ブラウザ smoke(テスト DB・1680x920、API シードでプロジェクト+512x384 ソース画像を投入):
  マスク編集 ON → ブラシストローク描画(13397px)と draft への commit(localStorage 反映)、
  消しゴム/ペン切替、反転・島除去・適用(enabled=true + メッセージ)、wheel ズーム
  (--mask-zoom 即時反映 + idle 後 zoomScale=1.12 永続化)、ブラシサイズ/feather スライダー、
  中ボタン画像パン(panOffset 永続化)、pose タブ往復、パネルリサイズ(300→340px 確定)、
  クリア、Escape 閉→再オープンで maskEditMode OFF を実操作で確認。コンソールエラーなし。
  ※ フローティング `.mask-toolbar`(ドラッグ/最小化)は現行 UI では描画されない残存コードで、
  挙動は従来と同一(verbatim 移動のみ)。

### フェーズE(2026-07-05 完了)

- ブランチ `refactor/phase-e-paint-controller`(worktree)→ main へマージ(95358b0)。
- 新設:
  - `src/client/paintEditorController.ts`(497 行): ペイントツールの state(layer/undo キャッシュ・
    アクティブストローク・alt スポイト・wheel zoom idle タイマー)・ストローク描画(rAF バッチ +
    dirty rect)・undo・ツール/色/ブラシサイズ設定・スポイト(通常 + Alt 一時切替)・クリア・
    モーダル同期(`syncAssetModalPaintCanvas`)を verbatim 移動。mask/pose/websam controller と
    同じ方式(`handlePaintEditorPointerDown/Move/Up/Cancel` / `handlePaintEditorKeydown/Keyup` /
    `handlePaintEditorBlur` を boolean 返しで公開、`closePaintEditorSession()` にモーダル閉時の
    一括破棄を集約)。`toggle-paint-editor` / `paint-tool` / `paint-color` / `paint-clear` /
    `paint-undo` の 5 action を `registerActions` で登録(main.ts の if/else から削除)。
  - 例外: `savePaintResultAsSourceAsset`(`paint-save`)のみ main.ts に残置。
    `refreshProject` / `generationDraftFromForm` / `applyAssetDimensionsToDraft` という
    generation lifecycle(フェーズF/H で未抽出)に依存するため、controller → main.ts の
    逆依存を作らないよう意図的に見送った(advisor 相談済み)。
- main.ts: 2913 → 2518 行(−395)。
- 検証: typecheck 0 エラー / test 319 件全パス / build / check 成功。
  ブラウザ smoke(テスト DB・1680x920): ペイントモード ON → ブラシ描画 → 消しゴム切替 →
  色変更 → Alt 一時スポイト(開始/解除) → Ctrl+Z → クリア → wheel ズーム(即時反映 + idle 後
  永続化) → 「別アセットとして保存」(paint-save、新 Round 生成) → モーダル閉→再オープンで
  paintEditMode OFF リセット、を実操作で確認。コンソールエラーなし。

### フェーズF(2026-07-05 完了)

- ブランチ `refactor/phase-f-generation-controller`(worktree)→ main へマージ(b3e68cd)。
- 新設:
  - `src/client/formUtils.ts`(32 行): `readForm` / `formValue` / `setFormValue` を verbatim 移動。
    generation 専用の state に依存しない汎用 `<form>` helper のため、generation ドラフト系とは
    別の小さな独立モジュールに分離した(将来 project/settings controller を切り出す際の
    逆依存を避けるため)。
  - `src/client/generationDraft.ts`(390 行): generation draft の pure data 関数群
    (`captureGenerationDraft` / `generationDraftFromForm` / `generationDraftFromRequest` /
    `setGenerationDraftValue` / `draftNumber` / `setPositivePromptDraft` /
    `setGenerationSliderDraft` / `syncPreviewPromptControl` / `inpaintRequestForParent` /
    `controlnetRequestForParent` / `workflowHasControlNetApply` / `updateDenoiseControlForMode` /
    `fillGenerationFormFromAsset` / `preserveDenoiseOnAssetFill` / `prepareGenerationFormForParent` /
    `applyAssetDimensionsToForm` / `applyAssetDimensionsToDraft` / `resolveTemplateForGeneration` /
    `preserveGenerationDenoise` / current\*Value ゲッター 12 種 / `getActiveRound` / `findRound` /
    `getActiveRoundAssets` / `getPreferredParentAsset` / `assetPassesFilter`)を verbatim 移動。
    action 登録や API 呼び出しを含まず、main.ts の render 系と generationController.ts の
    双方から参照される。`fillGenerationFormFromAsset` 内の直接 `render()` 呼び出しは
    他の controller と同じ規約に合わせて `requestRender()`(`./appState`)に置換。
  - `src/client/generationController.ts`(592 行): Round lifecycle・生成 API 呼び出し・action
    ハンドラを verbatim 移動(`generateRound` / `generateFromSelected` /
    `resolveParentAssetForGeneration` / `collectRound` / `interruptRound` / `deleteRoundTree` /
    `pollCollectRound` / `knownRoundAssetCount` / `responseRoundAssetCount` / `refreshProject` /
    `resumeAutoCollectForActiveRounds` / `isRoundActive` / `isRoundActiveStatus` /
    `terminalRoundMessage` / `setAssetStatus` / `toggleSelect` / `toggleFavorite` /
    `selectAllActiveRound` / `clearSelectionActiveRound` / `invertSelectionActiveRound` /
    `resetActiveRoundMarks` / `exportSelected` / `previewRoundDeletion` / `selectRound` /
    `randomSeed` / `swapResolution` / `scaleResolution` / `resolutionValue` / `roundToStep` /
    `resetGenerationParamsToTemplateDefaults`)。auto-collect polling 用の
    `pendingAutoCollectRoundIds` / `autoCollectIntervalMs` もこのファイルへ移動。
    `select-round` / `collect-round` / `interrupt-round` / `delete-round` /
    `cancel-delete-round` / `generate-round` / `img2img-next` / `generate-from-preview` /
    `paint-save` / `asset-selected` / `asset-rejected` / `asset-unmarked` / `toggle-select` /
    `toggle-favorite` / `select-all` / `clear-selection` / `invert-selection` /
    `export-selected` / `reset-session` / `reset-generation-params` / `random-seed` /
    `swap-resolution` / `scale-resolution` / `use-parent` の 24 action を `registerActions` で
    登録(main.ts の if/else から削除)。
  - フェーズE の意図的な例外だった `savePaintResultAsSourceAsset`(`paint-save`)を
    ここに移動。依存していた `generationDraftFromForm` / `applyAssetDimensionsToDraft` /
    `refreshProject` が本フェーズで抽出されたため、main.ts 残置の理由が解消した。
    移動に伴い、フェーズE時点で付けていた「フェーズF/H で未抽出のため残置」という
    説明コメントは削除(理由が過去形になり不要なため)。`paintEditorController.ts` から
    `activePaintCanvasAndAsset` / `commitActivePaintCanvas` / `getOrCreatePaintLayer` /
    `paintLayerCache` / `paintUndoStacks` を、`paintCanvas.ts` から
    `composePaintResultCanvas` を import する一方向依存(generationController → paintEditorController)
    のみで、逆依存は発生していない。
  - `src/client/clientUtils.ts` に `delay(ms)` を追加(main.ts から verbatim 移動)。
    `pollCollectRound`(generationController.ts)と main.ts の `copy-seed` ハンドラの双方が
    ここから import する。
- main.ts: 2518 → 1578 行(−940)。`renderProjectDetailView` / `renderGenerationPanelView` /
  `renderAssetModalView` / `renderHeader` / `getConnectionView` は composition root として
  main.ts に残置し、`getActiveRound` / `getActiveRoundAssets` / `assetPassesFilter` /
  `getPreferredParentAsset` / current\*Value ゲッター群は `generationDraft.ts` から、
  `isRoundActive` は `generationController.ts` から import するよう更新。
  `openProject` の `resumeAutoCollectForActiveRounds`、`uploadSourceAsset` /
  `deleteWorkflowTemplate` の `refreshProject`、`scheduleAssetCardSelect` /
  `scheduleIterationDotSelect` の `toggleSelect` / `selectRound` / `captureGenerationDraft` も
  同様に import 経由に置換(呼び出し箇所・順序は変更なし)。
- 検証: typecheck 0 エラー / test 319 件全パス / build / check 成功
  (このワークツリーは `node_modules` 未導入の状態で開始したため `npm install` を実行してから
  build/check を通した)。
  ブラウザ smoke(テスト DB・1680x920、API シードで txt2img テンプレート + Project を投入。
  ComfyUI 実機は接続済みだが checkpoint 未導入のため生成は 400/500 で失敗する構成 — round
  lifecycle の配線検証が目的で実画像生成の成否は対象外):
  txt2img 初回生成(`generate-round`、リクエスト送信 → ComfyUI エラーを `state.message` に
  正しく表示・`state.busy` 復帰、コンソールエラーなし)、source asset アップロードで
  img2img 用アセット作成(`uploadSourceAsset` → `refreshProject` 経由で Round 反映)、
  `img2img-next` / 画像詳細の「この画像からブランチング」(`generate-from-preview`、
  `commitActiveMaskCanvas` → `findAsset` → `generateRound` の順序を確認)、Round操作
  (`collect-round` で「まだ出力画像はありません」表示、`delete-round` の
  プレビュー→Escapeでの取消→再度ダブルクリック→確定削除、`select-round` の
  220ms シングルクリック確定を確認)、アセット操作(`toggle-select` /
  `toggle-favorite` / `select-all` / `clear-selection` / `invert-selection` /
  `reset-session` を状態クラスの変化で確認)、生成パラメータ操作(`random-seed` /
  `swap-resolution` / `scale-resolution`(拡大→縮小で往復)/ `reset-generation-params` を
  フォーム値の変化で確認)、ペイントフロー一式(`toggle-paint-editor` → canvas への
  pointer ストローク → `paint-save` で新規 source asset 作成・`state.paintEditMode` リセットを
  確認、`savePaintResultAsSourceAsset` の新 import 元(generationDraft.ts /
  generationController.ts / paintEditorController.ts)経由の呼び出しを実地検証)、
  アセットカード / イテレーションドットの dblclick 判定(220ms タイマー)を実操作で確認。
  `preview_console_logs`(error/all)は全操作を通じて出力なし。
