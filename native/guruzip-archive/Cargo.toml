[package]
name = "guruzip-archive"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zip = { version = "=8.6.0", default-features = false, features = ["deflate"] }

[profile.release]
lto = "thin"
strip = true
