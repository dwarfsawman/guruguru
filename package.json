{
  "name": "guruguru",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node dist/server/index.js",
    "dev": "node scripts/build.mjs && node dist/server/index.js",
    "check": "node scripts/build.mjs && node --check dist/server/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --no-warnings --experimental-transform-types --import ./scripts/register-test-hooks.mjs --test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "mermaid": "^11.16.0",
    "onnxruntime-web": "^1.27.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
