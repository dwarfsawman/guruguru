# GURUGURU 引継ぎ資料

最終更新: 2026-07-02

## 現在の状態

- リファクタリング第11フェーズ（client main.ts のマスク編集 / WebSAM / canvas layer 周辺の型・下書き helper・canvas helper の分割）を追加実施済み。
  - `InpaintDraft`・`SamMaskCandidate`・`MaskStrokeKind`・`ActiveMaskStroke`・`ActiveBoxPrompt`・`ActiveImagePan`・`MaskLayerSet`・`MaskBrushCursorKind` 型を `src/client/maskTypes.ts` に分離。`WebSamPoint` / `WebSamBox` / `WebSamModelStatus` / `WebSamPromptMode` / `WebSamProviderId` は `./websam/types` から、`MaskedContent` / `InpaintArea` は `../shared/types` から `import type` で参照する型のみの module。
  - マスク下書きの純粋 helper（`maskedContentOptions`・`defaultInpaintDraft`・`normalizeInpaintDraft`・`hasMaskData`・`hasActiveMaskData`・`isMaskedContent`）を `src/client/maskDraft.ts` に抽出。`maskedContent` 既定 `"original"`・`brushSize` 既定 `48`・`maskOpacity` 既定 `0.58`・旧 `maskDataUrl` しかない draft を `manualIncludeMaskDataUrl` に移す fallback・`hasMaskData` の `data:image/png;base64,` 判定は維持。
  - canvas / 幾何 / layer 合成 helper（`createLayerCanvas`・`clearCanvas`・`drawDataUrlIntoCanvas`・`renderFinalMaskToCanvas`・`composeFinalMaskDataUrl`・`maskLayerForStroke`・`paintStroke`・`canvasHasMaskPixels`・`pointerToMaskCanvasPoint`・`normalizePromptBox`・`sampleBrushPromptPoints`・`distanceToSegmentSq`）を `src/client/maskCanvas.ts` に抽出。最終マスク合成 `finalMask = (samMask OR manualInclude) AND NOT manualErase`・preview SAM mask 表示・`composeFinalMaskDataUrl(..., false)` で preview を確定 mask に含めない・手動ペンの `manualInclude` 描画と `manualErase` の `destination-out` 削除・消しゴムの `manualErase` 追加・白い `strokeStyle`/`fillStyle`・`lineCap`/`lineJoin` round・点描画の円処理は維持。重複 layer 初期化を減らすため `createMaskLayerSet` 小 helper を追加し、`ensureMaskLayerSet` / `getOrCreateMaskLayerSet` から呼ぶ。
  - `main.ts` は `state` 本体・event binding・API 呼び出し・`maskLayerCache`・`webSamWorker` と request id 類・`setInpaintDraft` / `ensureInpaintDraft` など state mutation・`commitMaskLayers`・`drawMaskSegment`・`removeBrushPromptPointsNearSegment`・`syncMaskLayerSetFromDraft`・`ensureMaskLayerSet` / `getOrCreateMaskLayerSet`・WebSAM worker lifecycle など stateful orchestration を残し、マスク編集の pure helper / 型を削減。`BRUSH_PROMPT_POINT_SPACING` / `BRUSH_PROMPT_MAX_POINTS`（48/48）は `main.ts` 側の orchestration config として維持。
  - `maskTypes.ts` / `maskDraft.ts` / `maskCanvas.ts` は `main.ts` を import しない（circular import なし）。依存は `main.ts` → 新 module の一方向。型のみの参照は `import type` を使用。
  - UI 文言・HTML 構造・CSS class・`data-action`・selector・API path・request/response shape・DB 保存形式・生成挙動・マスク合成の意味は変更していない。
- リファクタリング第10フェーズ（client main.ts の workflow/template/import/diagram/defaults 周辺の分割）を追加実施済み。
  - `WorkflowTemplate`・`WorkflowImportDraft`・`TemplateGenerationDefaults`・`TemplateModelDefaults` 型を `src/client/workflowTypes.ts` に分離。
  - workflow import / template export の純粋 helper（`defaultWorkflowImportRoleMap`・`defaultWorkflowImportDraft`・`slugify`・`parseWorkflowFileContent`・`workflowExportFilename`・`buildTemplateExportPayload`）を `src/client/workflowImport.ts` に抽出。`loadWorkflowFile()` の DOM/state 更新は `main.ts` に残し、parse 判定ロジック（JSON.parse / root object check / workflowJson・workflow_json・raw workflow の取り出し / roleMap・role_map・role_map_json の取り出し / `inferRoleMap()` fallback）と既存メッセージ文字列は pure helper に寄せた。
  - WorkflowTemplate から生成フォーム既定値を読む helper（`defaultModeForTemplate`・`templateGenerationDefaults`・`emptyModelDefaults`・`modelDefaultsFromWorkflow`・`firstStringInput`・`hasAnyInput`・`appendUnique`・`stringFromPath`・`numberFromPath`・`stringFromNodeInput`・`numberFromNodeInput`・`valueFromNodeInput`・`valueFromPath`）を `src/client/workflowDefaults.ts` に抽出。戻り値・fallback・参照 role map path の意味は維持。
  - Mermaid 初期化・`renderWorkflowDiagramCanvases`・`renderWorkflowImportPanel`・`renderTemplatePanel`・`renderWorkflowImportModal`・`renderWorkflowDiagramModal`・`renderWorkflowImportPreview`・`renderWorkflowDiagramBlock`・`renderWorkflowDiagramNotice`・`renderWorkflowTypeOptions`・`renderTemplateOption`・`renderModelReadout` を `src/client/workflowUi.ts` に抽出。state は引数で受け取り `main.ts` への逆依存を持たない（circular import なし）。Mermaid 設定値・`workflowDiagramRenderRunId` による stale render 防止・`data-mermaid-diagram` / `.workflow-diagram-source` / `.workflow-import-preview-slot` selector・`data-action`・HTML 構造・文言は維持。
  - `main.ts` は `state` 本体・event binding・API 呼び出し・`createTemplate`・`deleteWorkflowTemplate`・`loadWorkflowFile` の DOM/state 更新・modal open/close state mutation・generation/mask/WebSAM の既存処理を残し、WorkflowTemplate 周辺の pure helper / UI renderer を削減。
  - UI 文言・HTML 構造・CSS class・`data-action`・selector・API path・request/response shape・DB 保存形式・生成挙動は変更していない。
- リファクタリング第9フェーズ（round / generation lifecycle の抽出）を追加実施済み。
  - `createGenerationRound()`・`collectRound()`・`interruptRound()`・`deleteRoundTree()`・`ensureRoundMonitor()` と、それらが依存する type / const / private helper 一式を `src/server/rounds.ts` に抽出。
  - round monitor の in-memory state (`activeRoundMonitors`・`roundCollectionLocks`・`terminalJobStatuses`・`activeJobStatuses`・`terminalRoundStatuses`) は `rounds.ts` の module-level Map/Set として保持。
  - `errorToJson`・`stopRoundMonitor`・`handleComfySocketMessage`・`collectRoundUnlocked`・`getRoundForApi` など外部から呼ばない helper は `rounds.ts` 内の private helper とした。
  - `server/index.ts` は server 起動・`routeApi()` dispatch・settings API・WebSAM model proxy・shutdown handler だけを残し、round/generation 詳細実装を削除。round route は `./rounds` から import して呼ぶ形に変更。
  - `getProjectDetail(id, { ensureRoundMonitor })` の callback 注入は維持。`rounds.ts` から `index.ts` への import は存在しない（circular import なし）。
  - API path・status code・error message・response shape・DB 保存形式・ComfyUI workflow patch の意味は変更していない。
- リファクタリング第8フェーズ（source asset handler と周辺 helper の抽出）を追加実施済み。
  - `createSourceAsset()` を `src/server/sourceAssets.ts` に抽出。
  - generation request 正規化（`normalizeGenerationRequest()`、`clampInteger()`、`maxBatchSize`）を `src/server/generationRequest.ts` に抽出。
  - source/mask data URL 検証（`decodeImageDataUrl()`、`decodeMaskDataUrl()`、`normalizedUploadFileName()`）を `src/server/uploadDataUrl.ts` に抽出。
  - round index / branch color 採番（`nextRoundIndex()`、`branchAssignmentForRound()`）を `src/server/roundBranches.ts` に抽出。
  - `server/index.ts` は source asset API route の dispatch と generation orchestration / collect / monitor を残し、status code / error message / response shape は変更していない。
- リファクタリング第7フェーズ（project handler の抽出）を追加実施済み。
  - `listProjects()`、`createProject()`、`deleteProject()`、`getProjectDetail()` を `src/server/projects.ts` に抽出。
  - `getProjectDetail()` の running/pending round monitor 再開は `ensureRoundMonitor` callback を受け取る形にし、round 監視 machinery は `server/index.ts` に残した。
  - `server/index.ts` は project API route の dispatch と callback 注入だけを残し、status code / error message / response shape は変更していない。
- リファクタリング第6フェーズ（asset handler/helper の抽出）を追加実施済み。
  - `updateAssetStatus()`、`serveAssetFile()`、`decorateAsset()`、asset status 変更用の `selectionActionFor()` を `src/server/assets.ts` に抽出。
  - `server/index.ts` は asset API route の dispatch と生成・project detail からの `decorateAsset()` 利用だけを残し、status code / error message / response shape は変更していない。
- リファクタリング第5フェーズ（role map path helper の共有化と inpaint 既定値整合）を追加実施済み。
  - `nodeIdFromRolePath` を `src/shared/workflowRolePath.ts` に集約。`server/index.ts`、`server/workflow.ts`、`shared/workflowDiagram.ts` の role path 先頭 node 抽出を共有 import に切替。
  - `server/index.ts` の `normalizeMaskedContent()` fallback を client/UI と同じ `original` に変更。`maskedContent` 未指定の inpaint request でも元画像潜在を維持する。
- リファクタリング第4フェーズ（重複 helper の共有化と server/client の純粋 helper・handler の追加抽出）は完了済み。
  - `isJsonObject` / `Json` 型を `src/shared/json.ts` に集約。`server/validate.ts`、`client/json.ts`、`shared/workflowRoleMap.ts`、`shared/workflowDiagram.ts` の重複実装を廃止し import に切替。
  - server 側 template handler 群（`listTemplates`/`createTemplate`/`deleteTemplate` + 専属 helper `parseJsonInput`）を `src/server/templates.ts` に抽出。
  - server 側ファイル配信 helper 群（`serveStatic`/`streamFile`/`contentTypeFor` + `publicDir`）を `src/server/files.ts` に抽出。このとき verbatim 抽出。
  - ついでにディレクトリ URL (`/ort/` 等) で `serveStatic` が `createReadStream(ディレクトリ)` を起こして応答を返さずハングする既存バグを修正。ディレクトリは配信対象外として SPA fallback に回した。ファイル配信・traversal 保護・asset 配信は維持。
  - client 側 API wrapper (`api()`) を `src/client/api.ts` に抽出。
- リファクタリング第2フェーズ（P0: path containment 共通化）は完了済み。
- P1 の一部（generation mode 規則の共有化）は完了済み。
- 第3フェーズ（純粋 helper の抽出）を追加実施済み: server HTTP utility (`sendJson`/`readJson`/`HttpError`) → `src/server/http.ts`、server 検証/coercion helper → `src/server/validate.ts`、client format/escape helper → `src/client/format.ts`、client JSON object helper と `Json` 型 → `src/client/json.ts`。いずれも挙動変更なしの抽出。
- 第1フェーズでは `src/client/main.ts` 末尾にあった SVG icon helper 群を `src/client/icons.ts` に抽出した。その後、path/generation/json/http/validation/template/file/api/role path/asset/project helper の共有化・抽出も追加済み。
- 作業ツリーは clean の状態から引き継げる想定。開始時は必ず `git status --short` で確認する。

## 最重要の安全ルール

- 本番ユーザーデータを調査・要約・読み込みしない。
- `app.db`、生成画像、プロンプト、ワークフロースナップショット、サムネイルなどの runtime data はリポジトリ外に置く。
- DB を初期化しうるコマンドやサーバー起動は、検証時に必ず `GURUGURU_TEST_DB=1` を付ける。
- 安定したテスト用保存先が必要な場合は、リポジトリ外の `GURUGURU_TEST_DATA_DIR` を指定する。
- Codex がアプリを起動する場合、既定ポート `5177` は使わず、明示的に別 `PORT` を指定する。
- `git reset --hard`、`git checkout --`、未確認の破壊的削除は使わない。
- 既存の未コミット差分があれば、ユーザー変更として扱い、勝手に戻さない。

- リポジトリ構成

- `src/server/index.ts`: Node HTTP サーバーのエントリポイント。API routing (`routeApi()`)・settings API・WebSAM model proxy・shutdown handler に集中。round/generation lifecycle は `server/rounds.ts` に分離済み。
- `src/server/db.ts`: SQLite 初期化、設定、schema migration、row 変換、data directory 解決。
- `src/server/comfy.ts`: ComfyUI HTTP/WebSocket 連携、prompt queue、history/view/upload/interrupt。
- `src/server/storage.ts`: project storage、画像/マスク保存、asset file stream、project storage 削除。
- `src/server/assets.ts`: asset API handler（status 更新・画像/thumbnail 配信）と asset response URL 装飾。
- `src/server/sourceAssets.ts`: 手動 source asset upload API handler。completed round と selected asset を作成する。
- `src/server/generationRequest.ts`: generation request 正規化、batch size 上限、integer clamp。
- `src/server/uploadDataUrl.ts`: source image / mask PNG data URL のサイズ・MIME 検証と upload filename 正規化。
- `src/server/roundBranches.ts`: round index 採番と branch color/key 採番。
- `src/server/rounds.ts`: round / generation lifecycle。round 作成・collect・interrupt・delete・WebSocket monitor と、それらが依存する in-memory state・private helper 一式。
- `src/server/paths.ts`: ファイルパス境界判定（`isPathInside` / `isPathInsideOrEqual`）。
- `src/server/http.ts`: HTTP response/request utility（`sendJson` / `readJson` / `HttpError`）。
- `src/server/validate.ts`: request body の検証・coercion helper（`objectBody` / `requiredString` / `stringOr` / `numberOr` 等）。`isJsonObject` は `shared/json.ts` から再エクスポート。
- `src/server/templates.ts`: workflow template API handler（`listTemplates` / `createTemplate` / `deleteTemplate`）と専属 `parseJsonInput`。
- `src/server/projects.ts`: project API handler（`listProjects` / `createProject` / `deleteProject` / `getProjectDetail`）。detail 取得時の round monitor 再開は callback で呼ぶ。
- `src/server/files.ts`: 静的ファイル配信（`serveStatic` / `streamFile` / `contentTypeFor`）と `publicDir`。ディレクトリ URL は SPA fallback。
- `src/server/workflow.ts`: ComfyUI workflow patch、seed 解決、role map 正規化、inpaint/img2img workflow 変換。
- `src/client/main.ts`: SPA の状態、イベント、API 呼び出し、render、mask editor、WebSAM 制御の大半。第11フェーズでマスク編集の型・下書き helper・canvas helper を `maskTypes`/`maskDraft`/`maskCanvas` に分離済み。第10フェーズで workflow/template/import/diagram/defaults 周辺を `workflowTypes`/`workflowImport`/`workflowDefaults`/`workflowUi` に分離済み。
- `src/client/workflowTypes.ts`: 第10フェーズで抽出した WorkflowTemplate / workflow import / template generation defaults 型。
- `src/client/workflowImport.ts`: 第10フェーズで抽出した workflow import / template export の純粋 helper。
- `src/client/workflowDefaults.ts`: 第10フェーズで抽出した WorkflowTemplate から生成フォーム既定値を読む helper。
- `src/client/workflowUi.ts`: 第10フェーズで抽出した Mermaid 初期化・workflow diagram / template 周辺 UI renderer。
- `src/client/maskTypes.ts`: 第11フェーズで抽出したマスク編集 / WebSAM / canvas layer 周辺の型（`InpaintDraft`・`SamMaskCandidate`・`MaskStrokeKind`・`ActiveMaskStroke`・`ActiveBoxPrompt`・`ActiveImagePan`・`MaskLayerSet`・`MaskBrushCursorKind`）。型のみの module。
- `src/client/maskDraft.ts`: 第11フェーズで抽出したマスク下書きの純粋 helper（`maskedContentOptions`・`defaultInpaintDraft`・`normalizeInpaintDraft`・`hasMaskData`・`hasActiveMaskData`・`isMaskedContent`）。
- `src/client/maskCanvas.ts`: 第11フェーズで抽出した canvas / 幾何 / layer 合成 helper（`createLayerCanvas`・`createMaskLayerSet`・`clearCanvas`・`drawDataUrlIntoCanvas`・`renderFinalMaskToCanvas`・`composeFinalMaskDataUrl`・`maskLayerForStroke`・`paintStroke`・`canvasHasMaskPixels`・`pointerToMaskCanvasPoint`・`normalizePromptBox`・`sampleBrushPromptPoints`・`distanceToSegmentSq`）。
- `src/client/icons.ts`: 第1フェーズで抽出した SVG icon helper。
- `src/client/format.ts`: format/escape helper（`escapeHtml` / `escapeAttr` / `formatDate` / `formatNumber` / `formatCssNumber` / `formatSliderValue`）。
- `src/client/json.ts`: JSON object helper と `Json` 型（`parseJsonObjectText` / `isJsonObject` / `pickJsonObject`）。`Json`/`isJsonObject` は `shared/json.ts` から再エクスポート。
- `src/client/api.ts`: API wrapper (`api()`)。
- `src/client/websam/*`: WebSAM / SlimSAM-77 の model 定義、worker 型、ONNX Runtime worker。
- `src/shared/*`: client/server 共有型、constants、workflow diagram、role map 推論/検証。
- `src/shared/json.ts`: `Json` 型と `isJsonObject` 型ガード。client/server/shared の重複を集約。
- `src/shared/generationMode.ts`: generation mode ごとの denoise、parent asset 要否、parent relation 変換。
- `src/shared/workflowRolePath.ts`: role map path 文字列から node id を取り出す `nodeIdFromRolePath`。
- `scripts/build.mjs`: esbuild による server/client/worker bundle と static asset copy。

## エントリポイント

- server: `src/server/index.ts`
- client: `src/client/main.ts`
- WebSAM worker: `src/client/websam/worker.ts`
- build script: `scripts/build.mjs`
- public HTML: `src/client/index.html`

## 状態管理と UI

- client は framework なしの単一 SPA。
- `src/client/main.ts` の `state` object が主要な UI 状態を保持する。
- DOM は `render()` で `app.innerHTML` をまとめて再生成する。
- イベントは `bindEvents()` で document/app level に集約されている。
- iteration tree は DOM 再生成時に scroll position を保存・復元する設計。
- mask editor/WebSAM は同じ `main.ts` 内で、canvas layer、worker message、draft state を直接管理している。

## API/生成/ファイル処理の責務

- `routeApi()` が `/api/*` を一括 dispatch している。
- project handler は `server/projects.ts`、template handler は `server/templates.ts`、asset handler は `server/assets.ts`、round/generation handler は `server/rounds.ts`、source asset upload handler は `server/sourceAssets.ts` に分離済み。`server/index.ts` は route dispatch と settings/WebSAM/shutdown のみ。
- generation は `generation_rounds` と `generation_jobs` に分かれる。
- 通常生成は GURUGURU 側で `batch_size=1` の job に分割し、ComfyUI に複数 prompt を queue する。
- server は ComfyUI WebSocket と UI 側 collect polling の両方で生成結果を回収する。
- asset/mask の実ファイルは `dataRoot/projects/<projectId>/...` に保存される。
- static file serving は `server/files.ts`、asset file serving は `server/assets.ts` から `server/files.ts` / `server/storage.ts` を使う構成。

## 巨大ファイルと責務集中

調査時点の大きなファイル:

- `src/client/main.ts`: 4190 行（第1〜11フェーズで helper 抽出を進行中）
- `src/client/styles.css`: 2776 行
- `src/server/index.ts`: 301 行（第3〜9フェーズで HTTP/検証/template/file/role path/asset/project/source asset/round helper 抽出により縮小。round/generation lifecycle は `server/rounds.ts` へ移動）
- `src/server/rounds.ts`: 1091 行（第9フェーズで round/generation lifecycle を `server/index.ts` から抽出）
- `src/server/workflow.ts`: 726 行
- `src/client/websam/worker.ts`: 664 行

主な問題:

- `main.ts` は UI render、API client、状態遷移、canvas、WebSAM worker 制御、format helper が混在している。
- `server/index.ts` は HTTP routing、validation、DB query、generation orchestration、collection、static serving が混在している。
- `workflow.ts` は ComfyUI API workflow の自動補正が集中しており、変更の影響範囲が読み取りにくい。

## 型安全性の弱い箇所

- DB row/API row は `Record<string, unknown>` と `toApiRow()` に大きく依存している。
- `JSON.parse(... as T)` や手動 cast が複数ある。
- runtime validation は局所的で、request/response 全体を schema として固定していない。
- client と server で似たルールが別実装になっている。

重複が確認された helper:

- `defaultDenoiseForMode`: client/server → P1 で `src/shared/generationMode.ts` に集約済み
- `requiresFullDenoise`: client/server → P1 で `src/shared/generationMode.ts` に集約済み
- `isJsonObject`: client (`src/client/json.ts`) / server (`src/server/validate.ts`) / shared (`workflowRoleMap.ts`・`workflowDiagram.ts`) → 第4フェーズで `src/shared/json.ts` に集約済み
- `isPathInside`: `db.ts` / `storage.ts` → 第2フェーズで `src/server/paths.ts` に集約済み
- `nodeIdFromRolePath`: `server/index.ts` / `server/workflow.ts` / `shared/workflowDiagram.ts` → 第5フェーズで `src/shared/workflowRolePath.ts` に集約済み

## 検証コマンド

利用可能な npm scripts:

```powershell
$env:GURUGURU_TEST_DB='1'; npm run build
$env:GURUGURU_TEST_DB='1'; npm run check
```

- `npm run build`: esbuild bundle と static asset copy。
- `npm run check`: build 後に `node --check dist/server/index.js`。
- `typecheck`、`lint`、`test` script は現時点で未定義。
- Markdown のみの変更でも、最低限 `git diff --check` は実行する。

## 第1フェーズで実施済み

目的:

- 大きな挙動変更なしで `src/client/main.ts` の責務を少しだけ軽くする。
- レビューしやすい小さな単位にする。

変更:

- 新規: `src/client/icons.ts`
- 変更: `src/client/main.ts`
- `iconLoop` などの SVG helper を `main.ts` から移動。
- `main.ts` は同じ関数名を import する構成に変更。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功
- icon 関数本文の移動前後一致: 成功

## 次フェーズ候補

### P0: path containment の安全性整理 (完了)

対象:

- `src/server/storage.ts`
- `src/server/index.ts`
- `src/server/db.ts`

実施内容:

- 新規: `src/server/paths.ts` に `isPathInside`（厳密: target が parent の真の子）と `isPathInsideOrEqual`（同値も含む）を集約。どちらも `relative(resolve(parent), resolve(target))` ベース。
- `storage.ts` の局所 `isPathInside` を削除し import に切替。`safeFileStream` の大小文字無視 `startsWith` prefix 判定を `isPathInside` へ寄せた。
- `index.ts` の `serveStatic`（publicDir 境界）と `streamFile`（dataRoot 境界）の `startsWith` prefix 判定を `isPathInside` へ寄せた。
- `db.ts` の起動時 data dir 拒否は同値ケース（data dir == repo root）も拒否したいので `isPathInsideOrEqual` を使用。局所 `isPathInside` は削除。
- 挙動差: `safeFileStream`/`serveStatic` で「target == parent 丁度」は以前は通過→stream error だったが、厳密判定で境界外→fallback/throw になる。実運用（ディレクトリそのものを配信しない）には影響しない。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功
- `paths.ts` 単体: 子/同値/prefix-sibling/traversal/cross-drive の境界ケースを検証
- test server (PORT=5188, GURUGURU_TEST_DATA_DIR): `/api/health`、通常静的asset、`/../package.json` 等 traversal→index.html fallback、source-asset upload→`/api/assets/<id>/image` で dataRoot 内 PNG が正常配信、bogus id→404

### P1: server routing の分割

対象:

- `src/server/index.ts`

問題:

- `routeApi()` と周辺関数が大きく、template/project/round/asset/generation の責務が混在している。

改善案:

- まず HTTP utility (`sendJson`, `readJson`, `HttpError`) を小モジュールへ抽出。→ 完了 (`src/server/http.ts`)
- 続けて検証/coercion helper (`objectBody`/`requiredString`/`stringOr`/`numberOr` 等) を抽出。→ 完了 (`src/server/validate.ts`)
- template handler を抽出。→ 完了 (`src/server/templates.ts`)
- ファイル配信 helper を抽出。→ 完了 (`src/server/files.ts`)
- asset handler/helper を抽出。→ 完了 (`src/server/assets.ts`)
- project handler を抽出。→ 完了 (`src/server/projects.ts`)
- source asset upload handler と周辺 helper (generation request 正規化・data URL 検証・round/branch 採番) を抽出。→ 完了 (`src/server/sourceAssets.ts` 等)
- 次に round/generation handler を段階的に分ける。
- 1回の PR/commit で route の意味変更をしない。

リスク:

- status code、error message、response shape が変わると UI に影響する。

検証:

- 既存 build/check。
- test DB で `/api/health`, `/api/settings/comfy`, `/api/templates`, `/api/projects` の smoke。

### P1: project handler の抽出 (完了)

対象:

- `src/server/index.ts`
- `src/server/projects.ts`

実施内容:

- 新規: `src/server/projects.ts` に `listProjects`、`createProject`、`deleteProject`、`getProjectDetail` を抽出。
- `getProjectDetail` が running/pending round の monitor を再開する処理は、`ensureRoundMonitor` callback を option として受け取り、round 監視 machinery 自体は `server/index.ts` に残した。
- `server/index.ts` は project route dispatch と callback 注入だけを残した。
- UI/API/DB/保存形式、status code、error message、response shape には触れていない。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功
- test server (PORT=5297, GURUGURU_TEST_DATA_DIR): `/api/health`、`/api/settings/comfy`、`/api/templates`、`/api/projects` が 200

### P1: source asset handler と周辺 helper の抽出 (完了)

対象:

- `src/server/index.ts`
- `src/server/sourceAssets.ts`
- `src/server/generationRequest.ts`
- `src/server/uploadDataUrl.ts`
- `src/server/roundBranches.ts`

実施内容:

- 新規: `src/server/sourceAssets.ts` に `createSourceAsset` を抽出。completed round と selected asset を同期的に作成するため、round monitor・ComfyUI WebSocket には依存しない。
- 新規: `src/server/generationRequest.ts` に `normalizeGenerationRequest`・`clampInteger`・`maxBatchSize` (と専属 `normalizeSampling`/`normalizeDenoise`) を抽出。
- 新規: `src/server/uploadDataUrl.ts` に `decodeImageDataUrl`・`decodeMaskDataUrl`・`normalizedUploadFileName` (と専属 `bytesMatchMimeType`/`formatBytes`/size 定数) を抽出。source image と mask で共通の MIME 判定を同じモジュールに寄せた。
- 新規: `src/server/roundBranches.ts` に `nextRoundIndex`・`branchAssignmentForRound` (と専属 `nextBranchColorIndex`・`BranchAssignment` 型) を抽出。生成本体と source upload の両方から使う共有 helper。
- `server/index.ts` は source asset route dispatch と生成 machinery (round 作成・ComfyUI queue・collect・monitor) を残し、新規 helper を import する構成に変更。
- UI/API/DB/保存形式、status code、error message、response shape には触れていない。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功

### P1: asset handler/helper の抽出 (完了)

対象:

- `src/server/index.ts`
- `src/server/assets.ts`

実施内容:

- 新規: `src/server/assets.ts` に `updateAssetStatus`、`serveAssetFile`、`decorateAsset`、asset status 変更用 `selectionActionFor` を抽出。
- `server/index.ts` は asset route dispatch と生成・project detail からの `decorateAsset` 利用だけを残した。
- `serveAssetFile` は引き続き `streamFile()` を使うため、dataRoot 境界保護と 404 応答は既存のまま。
- UI/API/DB/保存形式、status code、error message、response shape には触れていない。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功

### P1: generation mode 規則の共有化 (完了)

対象:

- `src/client/main.ts`
- `src/server/index.ts`
- `src/shared/generationMode.ts`

実施内容:

- 新規: `src/shared/generationMode.ts` に `defaultDenoiseForMode`、`normalizeDenoiseForMode`、`requiresFullDenoise`、`requiresParentAsset`、`relationForGenerationMode` を集約。
- `client/main.ts` のローカル denoise helper と relation mapping を削除し、共有 module import に切替。
- `server/index.ts` のローカル denoise helper、parent upload 判定、relation mapping を削除し、共有 module import に切替。
- UI 文言、HTML 構造、API、DB、保存形式、生成結果には触れていない。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功

### P1: round / generation lifecycle の抽出 (完了)

対象:

- `src/server/index.ts`
- `src/server/rounds.ts`

実施内容:

- 新規: `src/server/rounds.ts` に round / generation lifecycle 一式を抽出。
  - public API: `createGenerationRound`・`collectRound`・`interruptRound`・`deleteRoundTree`・`ensureRoundMonitor`。
  - 移動した type: `HistoryImage`・`GenerationJobStatus`・`GenerationJob`・`CollectRoundResult`・`JobStats`。
  - 移動した module-level state: `terminalJobStatuses`・`activeJobStatuses`・`terminalRoundStatuses`・`activeRoundMonitors`・`roundCollectionLocks`。request ごとに作られる構造ではなく module-level Map/Set として保持。
  - 移動した private helper: `getRoundForApi`・`prepareInpaintRequest`・`normalizeInpaintOptions`・`normalizeMaskedContent`・`parentAssetDimensions`・`collectRoundUnlocked`・`collectLegacyRound`・`historyImagesForPrompt`・`storeGeneratedAsset`・`nextAssetBatchIndex`・`hasAssetsForPrompt`・`getGenerationJobs`・`requestForBatchJob`・`seedForBatchIndex`・`jobStats`・`updateRoundStatusFromJobs`・`interruptLegacyRound`・`promptIdsInQueueSections`・`collectPromptIds`・`handleComfySocketMessage`・`stopRoundMonitor`・`extractHistoryEntry`・`extractImages`・`selectFinalImages`・`finalImageNodeIds`・`isFinalImageOutputClass`・`addRoleNodeId`・`parseStoredJsonObject`・`errorToJson`。
  - `errorToJson` は `createGenerationRound` の catch 専用のため private helper 化。`stopRoundMonitor`・`handleComfySocketMessage`・`collectRoundUnlocked` 等も外部未使用のため非 export。
- `server/index.ts` は round/generation 詳細実装を削除し、round route を `./rounds` からの import 呼び出しに変更。`getProjectDetail(id, { ensureRoundMonitor })` の callback 注入は維持。
- 不要になった import (`readFile`・`createId`・`getRows`・`runSql`・`toApiRow`・`toApiRows`・ComfyUI queue/history/view/upload/interrupt/WebSocket・`readImageSize`/`storeImage`/`storeMaskImage`・`clampInteger`/`maxBatchSize`/`normalizeGenerationRequest`・`isJsonObject`/`stringOrNull`・`patchWorkflow`/`resolveSeed`・`decorateAsset`・`branchAssignmentForRound`/`nextRoundIndex`・`decodeMaskDataUrl`・`relationForGenerationMode`/`requiresParentAsset`・`nodeIdFromRolePath`・`GenerationMode`/`InpaintOptions`/`MaskedContent` 型) を `rounds.ts` 側へ移動。
- `rounds.ts` から `index.ts` への import は存在しない（circular import なし）。`rounds.ts` → `assets.ts` (`decorateAsset`) の依存はあるが `assets.ts` から `rounds.ts` への逆依存はない。
- UI/API/DB/保存形式、status code、error message、response shape、ComfyUI workflow patch の意味には触れていない。関数本文は verbatim 移動。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功
- test server (PORT=5391/5392, GURUGURU_TEST_DATA_DIR): `/api/health`・`/api/settings/comfy`・`/api/projects` が 200。bogus round に対する `/api/rounds/:id/collect`・`/interrupt`・`DELETE /api/rounds/:id` がいずれも `404 { error: "Round was not found" }` を返すことを確認（route dispatch と error message/status code が維持されている）。
- `rg` で `createGenerationRound`/`collectRound`/`interruptRound`/`deleteRoundTree`/`ensureRoundMonitor` の実装本体が `rounds.ts` にあり `index.ts` には route 呼び出ししかないこと、`activeRoundMonitors`/`roundCollectionLocks`/`GenerationJobStatus`/`HistoryImage` が `rounds.ts` のみに出現することを確認。

### P1: client main.ts の分割

対象:

- `src/client/main.ts`

問題:

- render、API、state mutation、canvas、WebSAM、format helper が1ファイルに集中している。

改善案:

- 挙動変更なしの純粋 helper から抽出する。
- 例: API wrapper、format/escape helper、generation mode helper、workflow import helper。
- DOM event や state mutation をまたぐ抽出は後回しにする。

進捗:

- format/escape helper (`escapeHtml`/`escapeAttr`/`formatDate`/`formatNumber`/`formatCssNumber`/`formatSliderValue`) → 完了 (`src/client/format.ts`)
- JSON object helper (`parseJsonObjectText`/`isJsonObject`/`pickJsonObject`) と `Json` 型 → 完了 (`src/client/json.ts`)
- API wrapper (`api()`) → 完了 (`src/client/api.ts`)
- workflow/template 型・import/export helper・生成フォーム既定値 helper・Mermaid/diagram UI renderer → 完了 (`src/client/workflowTypes.ts`/`workflowImport.ts`/`workflowDefaults.ts`/`workflowUi.ts`)。第10フェーズ。Playwright で browser smoke 済み。
- マスク編集の型・下書き helper・canvas helper → 完了 (`src/client/maskTypes.ts`/`maskDraft.ts`/`maskCanvas.ts`)。第11フェーズ。Playwright で browser smoke 済み。
- 残: mask editor・WebSAM worker 制御の stateful orchestration（`state` 本体・event binding・`maskLayerCache`・`webSamWorker` lifecycle・`commitMaskLayers`・`drawMaskSegment` 等）。DOM/state を密にまたぐ領域は `main.ts` に残す。

リスク:

- import 循環や初期化順の差で UI 起動に影響する。

検証:

- build/check。
- 必要に応じてテストDB + 非5177ポートで app を起動し、Home/Project/Workflow modal を browser smoke。

### P1: role map path helper の共有化 (完了)

対象:

- `src/server/index.ts`
- `src/server/workflow.ts`
- `src/shared/workflowDiagram.ts`

実施内容:

- 新規: `src/shared/workflowRolePath.ts` に `nodeIdFromRolePath` を集約。
- server 側の最終画像 node 判定、workflow patch 側の node fallback、Mermaid diagram の role node 強調で同じ helper を使うようにした。
- 既存関数と同じ `rawPath.split(".").filter(Boolean)[0]` の挙動を維持。
- 併せて `normalizeMaskedContent()` の fallback を `original` に変更し、client の inpaint 既定値と server の未指定時 fallback を揃えた。

検証済み:

- `GURUGURU_TEST_DB=1 npm run build`: 成功
- `GURUGURU_TEST_DB=1 npm run check`: 成功
- `git diff --check`: 成功

### P2: 型境界の整備

対象:

- `src/shared/types.ts`
- `src/server/db.ts`
- `src/server/index.ts`
- `src/client/main.ts`

問題:

- API response と DB row の型が広く、cast で通している箇所が多い。

改善案:

- まず shared API response 型を追加する。
- `toApiRow()` 直後の戻り値を必要箇所だけ狭める。
- `any` や強引な assertion で隠さない。

リスク:

- 一気に `tsconfig` strict 化すると範囲が広すぎる。

検証:

- 小さな型追加ごとに build/check。
- `tsc --noEmit` 導入は別フェーズで検討。

## 作業時の推奨手順

1. `git status --short` でユーザー差分を確認する。
2. 対象領域を `rg` と小さな file read で調べる。
3. 挙動変更がない抽出か、挙動変更を含む修正かを分ける。
4. 本番 data dir を読まないよう、DB 初期化が絡むコマンドは `GURUGURU_TEST_DB=1` を付ける。
5. 変更後は `git diff --check` と該当する npm script を実行する。
6. coherent な編集単位で commit する。

## 次に着手するなら

P0 は完了。第3フェーズで server/client の純粋 helper 抽出（HTTP utility、検証 helper、format/escape helper、JSON object helper）を完了済み。第4フェーズで `isJsonObject` の shared 化、server template handler・file serving helper の抽出、client API wrapper の抽出、ディレクトリ URL ハング修正を完了済み。第5フェーズで `nodeIdFromRolePath` の shared 化と `maskedContent` fallback 整合を完了済み。第6フェーズで asset handler/helper の抽出を完了済み。第7フェーズで project handler の抽出を完了済み。第8フェーズで source asset upload handler と周辺 helper の抽出を完了済み。第9フェーズで round / generation lifecycle の抽出を完了済み。第10フェーズで client `main.ts` の workflow/template/import/diagram/defaults 周辺を `workflowTypes`/`workflowImport`/`workflowDefaults`/`workflowUi` に分割済み。第11フェーズで client `main.ts` のマスク編集 / WebSAM / canvas layer 周辺の型・下書き helper・canvas helper を `maskTypes`/`maskDraft`/`maskCanvas` に分割済み。次は以下のいずれか。

- `server/index.ts`: 主要 handler 群は抽出済み。残るは `routeApi()` dispatch・settings API・WebSAM model proxy・shutdown のみで小規模。これ以上の分割は優先度低。
- `client/main.ts`: 残るのは stateful な orchestration（`state` 本体・event binding・API 呼び出し・modal open/close・generation/mask/WebSAM lifecycle・`maskLayerCache`・`webSamWorker` lifecycle・`commitMaskLayers`・`drawMaskSegment` 等）中心。第11フェーズでマスク編集の pure helper / 型は抽出済み。mask editor・WebSAM worker 制御・canvas layer 管理など DOM/state を密にまたぐ残りの領域は、state と入出力が密着するため `main.ts` 内に留める方が安全。これ以上の分割はリスクと相談。

最も安全なのは、挙動を変えない抽出を独立 commit で行うこと。
