# 操作メモ

GURUGURU の操作方法や注意点を追記するためのメモです。大きな仕様書ではなく、実際に開発・運用するときに必要な手順と落とし穴を残します。機能の内部実装・設計は `Docs/Reference-*.md`(随時更新するリファレンス)や `Docs/Done/`(完了機能の設計ログ)を参照してください。

## 開発時の基本操作

- 依存関係は `bun install` で用意します。Bun の想定バージョンは `package.json` の `packageManager` に合わせます。
- ビルドは `bun run build` を使います。
- 開発起動は `bun run dev` を使います。これはビルド後に `dist/server/index.js` を Bun で起動します。
- ビルドとサーバー起動 smoke を含む確認は `bun run check` を使います。
- 型チェックは `bun run typecheck`(`tsc --noEmit`、strict)を使います。コード変更後はゼロエラーを維持します。
- テストは `bun test` か `bun run test` を使います。
- CSS のブラウザ基準は **Chrome 150** を前提とします。`:has()`・`mask` 合成・CSS anchor positioning などの新しい CSS 機能をフォールバックなしで使ってよく、後方互換のための prefix / 代替実装は不要です(`src/client/styles.css` 冒頭コメントにも明記)。

## データ保存場所

- 通常実行時のユーザーデータは OS のユーザーデータディレクトリに保存されます。Windows では `%LOCALAPPDATA%\GURUGURU` が既定です。
- `GURUGURU_DATA_DIR` を指定する場合は、必ずこのリポジトリ外のパスにします。
- `app.db`、生成画像、プロンプト、ワークフロースナップショット、サムネイルなどのランタイムデータはリポジトリに置きません。
- リポジトリ直下や `data/` に実運用データベースを作らないでください。

## テスト・検証時の注意

- データベースを初期化するコマンドやテストでは `GURUGURU_TEST_DB=1` を設定します。
- テスト用データの保存先を明示したい場合は `GURUGURU_TEST_DATA_DIR` を使います。
- 本番のユーザーデータディレクトリや実運用の `app.db` を、テスト・調査・要約目的で読み込まないでください。
- コマンドがリポジトリ内のデータディレクトリを拒否した場合は、外部のテスト用ディレクトリを指定してやり直します。
- 既定ポート `5177` はユーザーが `bun run start` などで起動する通常インスタンス用に空けておきます。エージェントが検証用にアプリを起動する場合は `PORT` に 5177 以外を指定し、`GURUGURU_TEST_DB=1` とリポジトリ外の `GURUGURU_TEST_DATA_DIR` を使います。
- 複数インスタンスを同時に動かす場合は、ポートだけでなくデータディレクトリも分けます。同じ本番データディレクトリを複数プロセスから使う運用は避けます。
- UI レイアウト検証では、サイドバーやモーダルの崩れを確認するためにブラウザ viewport を一時的にワイド表示(例: 1680x920)へ切り替えてよいです。確認後は viewport 設定を戻します。
- `curl` でサーバー疎通確認するときは必ず `--max-time 5` を付ける。dev server・watch・test・Bun script などハングしうるコマンドは長時間待たず、1つの確認が 5〜10秒以上返らない場合は中断して原因を報告する。サーバー疎通確認では全 URL が必ずレスポンスを返すことを確認する(ディレクトリ URL でハングしないこと)。
- 本番 ComfyUI(既定ポート 8188)も本番ユーザーデータとして扱う。生成内容が見えるエンドポイント — `/history`(ワークフロー・プロンプト・シード)、`/view`(画像)— は読まず、input/output ディレクトリも閲覧しない。疎通確認レベル(`/queue`・`/system_stats`・`/object_info`)は可。エラー診断は、UI に表示されるエラーメッセージの共有をユーザーに依頼するか、下記のテスト用 ComfyUI インスタンスで再現して行う。本番インスタンスへの状態変更(`/free`・`/interrupt` 等)は事前にユーザーへ確認する。
- スマート選択(WebSAM)は既定で `/api/websam-models` から GitHub Release を取得します。本リポジトリは private のため、サーバー起動時に `GURUGURU_GITHUB_TOKEN` / `GH_TOKEN` / `GITHUB_TOKEN` のいずれかを設定してください(token はサーバー内だけで使い、ブラウザへは渡しません)。

## テスト用 ComfyUI インスタンス(DL 済み Desktop 版の流用)

生成パイプライン(workflow patch・ControlNet・統合 switch 等)の実機検証は、本番 ComfyUI(8188)ではなく、ダウンロード済み Desktop 版 ComfyUI のコードと Python 環境を流用した分離インスタンス(port 8288)で行います。

- コード: `$env:LOCALAPPDATA\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI`
- Python: 同ディレクトリの `standalone-env\python.exe`
- モデルは `%APPDATA%\Comfy Desktop\shared_model_paths.yaml` 経由で Desktop 版と共有(ダウンロード不要)

```powershell
# 1. リポジトリ外にテスト用 base ディレクトリを用意(custom_nodes が無いと起動時 FileNotFoundError)
$base = "$env:TEMP\comfy-test-8288"
New-Item -ItemType Directory -Force "$base\custom_nodes", "$base\input", "$base\output", "$base\temp"
# 統合 switch 検証では未使用枝の LoadImage にも実在ファイルが必要 → input にダミー画像を置く

# 2. 起動
$comfy = "$env:LOCALAPPDATA\Comfy-Desktop\ComfyUI-Installs\ComfyUI"
& "$comfy\standalone-env\python.exe" "$comfy\ComfyUI\main.py" `
  --port 8288 --base-directory $base `
  --extra-model-paths-config "$env:APPDATA\Comfy Desktop\shared_model_paths.yaml"

# 3. 疎通確認
curl --max-time 5 http://127.0.0.1:8288/system_stats
```

- GURUGURU 側はテストインスタンス(`PORT` ≠ 5177・`GURUGURU_TEST_DB=1`・外部 `GURUGURU_TEST_DATA_DIR`)を起動し、設定画面の ComfyUI URL に `http://127.0.0.1:8288` を設定して接続します。
- GPU 負荷を抑えるため、検証は低負荷パラメータ(例: 512x288 / 6 steps)で行います。
- テストインスタンスの output/input はテスト専用なので閲覧・削除して構いません。
- Desktop 版更新後に `standalone-env` から torch 等が消えることがあります。`requirements-nvidia.txt`(ピン留め torch)と `ComfyUI/requirements.txt` を `standalone-env\python.exe -m pip install -r` で復元すれば起動できます。

## マスク・ポーズ添付、画像貼り付け

- マスク編集・inpaint・ポーズ ControlNet 添付の内部実装(合成ルール・`patchInpaintLatentPath`・roleMap 接続トレース等)は [`Docs/Reference-MaskAndPoseAttachments.md`](Docs/Reference-MaskAndPoseAttachments.md) を参照してください。
- 画像貼り付け(Paste & Transform)は、D&D / Ctrl+V / サムネイルドラッグでアセットへ画像を「添付」でき、生成時にクライアントが見たまま合成して送信します(親画像・ツリー構造は不変)。設計・実装の詳細は [`Docs/Done/Feature-ImagePaste.md`](Docs/Done/Feature-ImagePaste.md) を参照してください。
- 落とし穴: 合成 PNG が 16MB を超えると生成前にクライアント側で中断されます(トースト表示)。`paste_sources/` のファイルは手動で削除しないでください(過去ラウンドの `request_json` からも参照されます。不要になったファイルはサーバ起動時に自動掃除されます、`purgeOrphanPasteSources`)。外部サイトからの画像 URL ドラッグ(ファイル実体なし)は taint 回避のため拒否されます。

## 画像詳細ビューア

- マスクツールバーの最小化・移動、ズーム/パンなどの挙動は [`Docs/Reference-AssetDetailViewer.md`](Docs/Reference-AssetDetailViewer.md) を参照してください。

## 逐次生成・停止

- バッチのジョブ分解・WebSocket 監視・停止時の挙動は [`Docs/Reference-GenerationPipeline.md`](Docs/Reference-GenerationPipeline.md) を参照してください。

## イテレーションツリー

- Workflow diagram(Mermaid)表示、スクロール位置保持の挙動は [`Docs/Reference-IterationTree.md`](Docs/Reference-IterationTree.md) を参照してください。

## ドキュメント運用

- 新しい操作手順、注意点、調査で分かった落とし穴はこのファイルに追記します。
- 機能の内部実装・挙動(コードを触る前に知っておくべき設計)は `Docs/Reference-*.md` に書きます。このファイルは手順と落とし穴だけを短く保ちます。
- リポジトリに関する Markdown を更新したときは、変更履歴にも要点を追記します。
- 古い大きな仕様書を復活させるのではなく、実際に役立つ運用メモとして短く保ちます。
- 変更履歴は直近分のみ詳細に残し、古い分は年月単位の要約へ畳んで肥大化を防ぎます。技術的な詳細は変更履歴に書かず、該当する `Docs/Reference-*.md` や `Docs/Done/*.md` に書きます。

## 変更履歴

- 2026-07-09: **コマ内生成(パネルターゲット生成)機能を実装**。コマ割りページのカードクリックでコマ選択 lightbox が開き、コマ選択→「選択コマを生成」で生成画面へ遷移(コマのアスペクト比を width/height 初期値へ反映)。生成画像を選択すると対象コマへ自動割り当て、lightbox 上でクリップ表示・ダブルクリックでクロップ編集(ドラッグで表示位置調整、pointerup で自動保存)。`bun run typecheck` 0・`bun test` 420/420(新規11件)・`bun run build` グリーン、ヘッドレスAPI 24アサーション、ブラウザ実機(1680×920)でlightbox〜クロップ保存の一連の流れをコンソールエラー無しで確認。詳細は [`Docs/Feature-PanelGeneration.md`](Docs/Feature-PanelGeneration.md)。
- 2026-07-09: 実行基盤を Bun へ全面移行。`packageManager: bun@1.3.14`、`bun.lock`、`bun:sqlite`、Bun bundler に切り替え、テストは `bun test`、確認は `bun run check`(ビルド+テストDBでの `/api/health` smoke)を使う。
- 2026-07-07: **Consistent Character に「スタイル LoRA」選択機能を追加**(絵柄コントロール用に `models/loras` の LoRA を複数スタック可・強度0〜2指定で MODEL チェーンへ注入。最大4本)。あわせて速度用の自動 Hyper LoRA(`lora` feature)は撤去 — 低ステップ化 LoRA は絵柄制御をしない上、ユーザーが LoRA を `chroma/` サブフォルダへ整理した結果、固定ベース名の自動挿入が ComfyUI の choices(サブフォルダ込み)と食い違い破綻するため。新設: `GET /api/comfy/loras`・`GenerationRequest.loras`・生成フォーム「スタイル LoRA」枠(`styleLoraController.ts`)。LoRA 名は ComfyUI の実 choice 文字列(サブフォルダ込み)を verbatim 使用するのでサブフォルダでも動く。`npm run typecheck` 0・`npm test` 380/380、実機(8288)で `chroma\Chroma_Voyager_86.safetensors` を選び txt2img 実生成 completed を確認。詳細は [`Docs/Feature-ConsistentCharacter.md`](Docs/Feature-ConsistentCharacter.md)。
- 2026-07-07: **Consistent Character の全体スタイル参照(IP-Adapter)+連動 RMBG を撤去**し、参照画像系を PuLID(顔)+ Hyper LoRA(自動挿入)に集約。IP-Adapter(x-flux)は現行 ComfyUI のネイティブ Chroma が per-block `img_mod` を持たず実行時非互換(core fp8 / city96 GGUF / multigpu の3ローダで再現確定。元ワークフローが動いたのは旧 ComfyUI が Chroma を Flux 扱い=`img_mod` ありでロードしていた時代)、代替の siglip/unCLIP も Chroma が `unclip_conditioning` を読まず no-op(`comfy/model_base.py` 確認)。絵柄コントロールは LoRA に委ねる方針。`npm run typecheck` 0・`npm test` 378/378。詳細は [`Docs/Feature-ConsistentCharacter.md`](Docs/Feature-ConsistentCharacter.md) の「実施記録(2026-07-07): IP-Adapter/RMBG 撤去」。
- 2026-07-07: **Consistent Character (Chroma) 機能を実装完了(Phase 0〜5)**。顔スタイル参照(PuLID-Flux)・全体スタイル参照(IP-Adapter)・Hyper LoRA・RMBG を、導入済みモデル/ノードパックに応じて自動ON/OFFするフラグメント注入方式(`src/server/workflowFeatureFragments.ts`)。生成フォームの親画像枠直下に「参照画像」枠を新設(`referenceController.ts`)、install モーダルを feature 別カード化。テスト用 ComfyUI(8288)へ PuLID-Flux/x-flux-comfyui を実導入し、参照画像付き txt2img で **PuLID 顔同一性転写を伴う実生成完走**まで検証済み(16枚バッチ completed・request_json の dataUrl 除去確認)。詳細は [`Docs/Feature-ConsistentCharacter.md`](Docs/Feature-ConsistentCharacter.md)。落とし穴: InsightFace antelopev2 の自動DLは `models/insightface/models/antelopev2/antelopev2/*.onnx` と二重ネストになり `assert 'detection' in self.models` で落ちることがあるので、`.onnx` を1階層上へフラット化する。
- 2026-07-07: Consistent Character (Chroma) 機能取り込みを起票(`Docs/Feature-ConsistentCharacter.md`)。顔スタイル参照(PuLID)・全体スタイル参照(IP-Adapter)・Hyper LoRA・RMBG を導入済みモデル/ノードパックに応じてON/OFFするフラグメント注入方式。
- 2026-07-07: イテレーションツリーのエッジポップアウトを修正し、貼り付け画像・マスク形状・ポーズ画像の拡大表示とレイヤ前面化に対応。img2img親画像選択を1枚に制限し、マスク編集の消しゴムカーソルと Ctrl/Cmd+Z Undo を修正。
- 2026-07-07: **モデル選択(Chroma)機能を実装**。テンプレート登録UI(インポートモーダル)と WorkflowTemplate一覧パネル(diagram/export/削除・Mermaid diagram機能一式)を削除し、同じ場所に「モデル選択」+ Chroma ボタン + 必要モデルインストール案内モーダル(`GET /api/comfy/model-check?family=chroma`)を追加。詳細は [`Docs/Done/Feature-ModelRequirementsCheck.md`](Docs/Done/Feature-ModelRequirementsCheck.md)。
- 2026-07-06: 機能別内部実装セクションを `Docs/Reference-*.md` へ分離(本ファイルは手順・落とし穴・直近履歴のみに縮小)。分離時に失われていた記述(Round ごとの編集記憶・「ノード元値」・draft 永続化 debounce・停止/失敗ドット・進捗スロットル)を各 Reference へ復元し、[`Docs/Reference-GenerationForm.md`](Docs/Reference-GenerationForm.md) を新設。
- 2026-07-06: マスク/ポーズの「次回生成に添付」チェックボックスをランプ型トグル(`.tab-attach-lamp`)に統一。詳細は [`Docs/Reference-MaskAndPoseAttachments.md`](Docs/Reference-MaskAndPoseAttachments.md)。
- 2026-07-06: **画像貼り付け(Paste & Transform)機能を実装**し、フィードバック一括対応(グリッド合成表示・エッジポップアウト操作性・焼き込み機能の削除・WebSAM入力の合成化・貼り付けソースの起動時GC ほか)まで完了。詳細は [`Docs/Done/Feature-ImagePaste.md`](Docs/Done/Feature-ImagePaste.md)。
- 2026-07-05: 第二次リファクタリングが全フェーズ(A〜J-5・UX改善#1〜#8)+総括コードレビューまで完了。詳細は [`Docs/Done/第二次リファクタリング計画.md`](Docs/Done/第二次リファクタリング計画.md)。
- 2026-07-05: CSS のブラウザ基準を Chrome 150 前提と明記(上記「開発時の基本操作」参照)。イテレーションツリーのエッジにホバーポップアウト(生成プロパティ表示)を追加。
- 2026-07-02〜07-03 まとめ: ポーズ ControlNet 機能一式(モデル選択・複数人対応・骨長拘束・img2img併用)、統合 Switch ワークフロー方式が完了(詳細は `Docs/Done/Feature-Pose*.md`・`Docs/Done/Feature-UnifiedSwitchWorkflow.md`)。本番 ComfyUI を本番ユーザーデータとして扱う規約を追加(上記「テスト・検証時の注意」参照)。ローカル永続化・サイドバー折りたたみ等の UI フィードバック対応。第一次リファクタリング完了(詳細は `Docs/Done/RefactoringLog2026-07-02.md`)。
- 2026-07-01 まとめ: 初期整備(規約整備、マスク編集基盤、逐次生成基盤、ビューアUX基盤)。詳細は `Docs/Done/RefactoringLog2026-07-02.md`。
