# 操作メモ

GURUGURU の操作方法や注意点を追記するためのメモです。大きな仕様書ではなく、実際に開発・運用するときに必要な手順と落とし穴を残します。

## 開発時の基本操作

- 依存関係は `npm install` で用意します。
- ビルドは `npm run build` を使います。
- 開発起動は `npm run dev` を使います。これはビルド後に `dist/server/index.js` を起動します。
- 構文チェックを含む確認は `npm run check` を使います。
- 型チェックは `npm run typecheck`（`tsc --noEmit`、strict）を使います。コード変更後はゼロエラーを維持します。
- 実行には Node.js 24 以上が必要です。

## データ保存場所

- 通常実行時のユーザーデータは OS のユーザーデータディレクトリに保存されます。Windows では `%LOCALAPPDATA%\GURUGURU` が既定です。
- `GURUGURU_DATA_DIR` を指定する場合は、必ずこのリポジトリ外のパスにします。
- `app.db`、生成画像、プロンプト、ワークフロースナップショット、サムネイルなどのランタイムデータはリポジトリに置きません。
- リポジトリ直下や `data/` に実運用データベースを作らないでください。

## テスト・検証時の注意

- データベースを初期化するコマンドやテストでは `GURUGURU_TEST_DB=1` を設定します。
- テスト用データの保存先を明示したい場合は `GURUGURU_TEST_DATA_DIR` を使います。
- 本番のユーザーデータディレクトリや実運用の `app.db` を、テスト・調査・要約目的で読み込まないでください。
- コマンドがリポジトリ内のデータディレクトリを拒否した場合は、外部のテスト用ディレクトリを指定してやり直します。
- 既定ポート `5177` はユーザーが `npm start` などで起動する通常インスタンス用に空けておきます。Codex が検証用にアプリを起動する場合は `PORT` に 5177 以外を指定し、`GURUGURU_TEST_DB=1` とリポジトリ外の `GURUGURU_TEST_DATA_DIR` を使います。
- 複数インスタンスを同時に動かす場合は、ポートだけでなくデータディレクトリも分けます。同じ本番データディレクトリを複数プロセスから使う運用は避けます。
- UI レイアウト検証では、サイドバーやモーダルの崩れを確認するためにブラウザ viewport を一時的にワイド表示（例: 1680x920）へ切り替えてよいです。確認後は viewport 設定を戻します。
- `curl` でサーバー疎通確認するときは必ず `--max-time 5` を付ける。dev server・watch・test・npm script などハングしうるコマンドは長時間待たず、1つの確認が 5〜10秒以上返らない場合は中断して原因を報告する。サーバー疎通確認では全 URL が必ずレスポンスを返すことを確認する（ディレクトリ URL でハングしないこと）。

## 画像マスク・inpaint

- 通常の img2img は、親画像を指定した幅・高さへ `ImageScale` でリサイズしてから `VAEEncode` に渡す。フォームの幅・高さは EmptyLatent ではなく、このリサイズノードで実際の出力サイズに反映される。
- マスク付き inpaint は、保存時のマスク寸法が親画像と一致していることを検証したうえで、生成時に親画像とマスクを同じ指定幅・高さへリサイズしてから `VAEEncode` / 合成へ渡す。
- 画像詳細ビューアのマスク編集は、親画像と同じサイズの PNG マスクだけを生成リクエストに使います。
- GURUGURU が生成するマスク PNG は透明背景に白で描画します。ComfyUI の `LoadImageMask` は alpha を反転マスクとして読むため、workflow patch では `red` チャンネルを使い、白い描画部分を更新対象にします。
- スマート選択で WebSAM / SlimSAM-77 を使う場合、既定ではローカルAPI `/api/websam-models` 経由で GitHub Release `websam-models-v1` の `slimsam-77-encoder.onnx` と `slimsam-77-decoder.onnx` を取得します。別の配布先を使う場合だけ、設定画面の `WebSAM model base URL` を変更します。
- このリポジトリは private のため、既定の `/api/websam-models` で release asset を読むには GURUGURU サーバー起動時に `GURUGURU_GITHUB_TOKEN`、`GH_TOKEN`、または `GITHUB_TOKEN` を設定します。token はブラウザへ渡さず、サーバー側で GitHub Release API を呼ぶためだけに使います。
- サーバーの release asset proxy（`src/server/index.ts` の `serveReleaseAsset`）は「ファイル名 → release API URL」のレジストリ方式です。WebSAM 用（`slimsam-77-encoder.onnx` / `slimsam-77-decoder.onnx` → `websam-models-v1`）に加え、ポーズ検出モデル用（`pose_landmarker_full.task` → GitHub Release `pose-models-v1`、`GET /api/pose-models/:filename`）を同じハンドラで配信します。token 要件・404/503 の挙動・streaming・cache-control は用途間で共通です。
- SlimSAM-77 のモデルファイルはブラウザの OPFS にキャッシュされます。2回目以降はキャッシュ済みファイルを優先します。OPFS非対応ブラウザでは手動マスク機能はそのまま使えますが、WebSAMのキャッシュは使えません。
- スマート選択の最終マスクは `finalMask = (samMask OR manualIncludeMask) AND NOT manualEraseMask` として合成し、この白黒PNGだけを `maskDataUrl` として送ります。SAM候補プレビューは「適用」するまで `samMask` として確定しません。
- 手動ペン（`manual-include`）で描くときは `manualInclude` レイヤーに加えて同じストローク形状を `manualErase` レイヤーから `destination-out` で削除する。これにより「消しゴムで消した領域を後からペンで再描きして復活させる」ことができる。消しゴムは引き続き `manualErase` に追加し、SAM結果や手動追加を最終合成で抜く。
- WebSAM Brush prompt のサンプリング間隔は `BRUSH_PROMPT_POINT_SPACING`（48px）/ `BRUSH_PROMPT_MAX_POINTS`（48点）で間引く。マジックナンバーではなく定数化し、過剰な点で decode が重くなるのを防ぐ。消しゴムによる点削除（`removeBrushPromptPointsNearSegment`）はブラシ半径ベースなので間引いた後も整合する。
- マスク編集時はブラシサイズ相当の半透明円（`.brush-cursor`）を画像座標系で追従表示する。ペン=緑、消しゴム=赤、Brush prompt=青。Point/Box モードでは非表示。ズーム・パンでも画像座標とズレない（SVG overlay が `mask-zoom-stage` 内で一緒にスケールする）。
- inpaint の `maskedContent` 既定は `original`（元画像の潜在を維持 + `SetLatentNoiseMask`）。`fill`（`VAEEncodeForInpaint` でマスク部をゼロ埋め）は低デノイズで灰色ベタが残りやすいため既定から外した。UIの選択肢ラベルで灰色化の有無を明示している。
- `patchInpaintLatentPath` では、サンプラー用ノイズマスク（`GrowMask` 済み・padding 含む）と、最終 paste back 用 `ImageCompositeMasked` のマスク（元の非拡大マスク）を分離する。これにより padding 領域へ生成結果が意図より広く貼り戻されるのを防ぎ、灰色化を悪化させない。
- デノイズ強度はラウンド切替や親画像切替でも保持する。`selectRound` はフォーム全体を破棄せず `denoise` だけ引き継ぐ。`fillGenerationFormFromAsset` は既存の denoise を上書きしない（txt2img など `requiresFullDenoise` のモードを除く）。`renderGenerationPanel` の表示値は `normalizeDenoiseForMode` で正規化し、実際に送信される `request.denoise` と一致させる。
- `generation_rounds.request_json` には `maskedContent`、`inpaintArea`、`onlyMaskedPadding` と保存済みマスク参照だけを残し、巨大な `maskDataUrl` は保存しません。
- マスク画像は各 Project の外部データディレクトリ配下 `projects/<projectId>/masks/` に保存されます。本番DBや本番データを調査せず、検証時は必ず外部テストデータディレクトリを使ってください。
- 画像カードの `MASK` 表示は、次回の img2img ブランチング request にマスクが実際に入る状態だけに付けます。マスク付き生成から生まれた画像でも、その画像自身に有効なマスク下書きがなければ通常画像として扱います。
- マスク編集を OFF にした場合、描いたマスク画像は下書きとして残しても request には含めず、`MASK` 表示も外します。再度 ON にすると残っている下書きを編集・適用できます。

## ポーズ ControlNet 添付

- 画像詳細ビューアのマスク編集サイドバーに「マスク / ポーズ」タブがあり、ポーズタブでは MediaPipe Pose Landmarker（`pose_landmarker_full.task`、GitHub Release `pose-models-v1` から `/api/pose-models` 経由で取得・OPFS キャッシュ）で検出した OpenPose 18点を関節ドラッグ・クリックで編集できる。編集対象は常に OpenPose 18点（MediaPipe 33 landmarks は検出直後に変換して破棄）。
- 「次回生成に添付する」を ON にした状態で生成すると、`renderPoseSkeletonDataUrl` が黒背景 + OpenPose 標準配色のスケルトン PNG を描画し、`request.controlnet.poseImageDataUrl` として送信する。**送信先テンプレートの workflowJson に `ControlNetApplyAdvanced` ノードが無い場合は添付自体を行わない**（クライアント側 capability チェック、`workflowHasControlNetApply`）。
- サーバーはマスクと同型の添付パイプラインで処理する: `decodeControlImageDataUrl`（PNG限定・8MB上限）→ `storeControlImage`（`projects/<projectId>/control/<roundId>_pose.png`）→ `uploadImageToComfy` → `patchControlNetPath`。`request_json` へ保存する前に `poseImageDataUrl` は null 化し、巨大 dataURL を DB に残さない（マスクの `maskDataUrl` null 化と同じ規約）。
- `patchControlNetPath` は `ControlNetApplyAdvanced` ノードを `roleMap.controlnet_apply_node`（無ければクラス名検索）で特定し、その `inputs.image` の**接続を辿って**供給元 LoadImage ノードを見つける（class 検索ではなく connection トレース。テンプレートに複数 LoadImage がある場合の取り違え対策）。`patchWorkflow` 本体の親画像一括注入（`load_image_input` 等）より**後**に実行されるため、pose 添付がある場合は同じノードを指していても pose 画像が必ず勝つ。ControlNet 非対応テンプレートでは何もせず optional に振る舞う。
- グリッドの `POSE` バッジ（紫、`.pose-badge`）は MASK バッジと同じ「次回生成に添付される」draft 駆動のセマンティクスで、`showMaskGridTag` トグルに乗せている。
- 初期リリースはテンプレートに `ControlNetApplyAdvanced` があるかどうかのみで添付可否を判定する。img2img（`VAEEncode` 系ノードが存在しないテンプレートで `vae_encode_image_input` が `ControlNetApplyAdvanced.inputs.image` に誤推論されるケース）との組み合わせは対応対象外（`Docs/Feature-PoseControlNet.md` §5「img2img モードとの組合せ」参照）。

## プレビューダイアログの最小化・移動

- 画像詳細ビューアの `.mask-toolbar`（プロンプト・マスク設定）は邪魔にならないよう最小化・移動できる。
- `state.maskToolbarMinimized` が true のとき本体を隠し、ヘッダーと展開ボタンだけを残す。
- ダイアログ表示中（非最小化）は `.mask-canvas` に `mask-locked` を付け `pointer-events: none` にし、マスク描画を無効化する。最小化するとマスクが効く。
- マスク編集モードに入った直後はツールバーを自動で最小化する。縦長画像では展開済みツールバーが画像全体を覆い、プロンプト等の文字要素が選択されてしまうため。プロンプト・設定を編集したいときは展開ボタンで戻す。
- ダイアログ外（画像・背景など）のタップで最小化する。ただし `[data-action]` や `.mask-toolbar` 内のタップは最小化しない。最小化済みのとき背景タップは従来通りモーダルを閉じる。
- ヘッダー (`[data-mask-toolbar-handle]`) のドラッグで `position: fixed` に切り替えて移動し、`state.maskToolbarPos` に保存する。`openAssetDetail` / `closeAssetDetail` / `toggleMaskEditor` で位置と最小化状態をリセットする。
- マスク編集の ON/OFF は、画像詳細ビューア上部の画像外コントロールバーにある常設ボタンで切り替える。マスクキャンバス上に切り替え文字を重ねない。
- 最小化中のツールバー見出しはドラッグ対象にせず、展開ボタン以外はポインターを透過させてマスク描画を妨げない。
- マスク編集中は画像上部に「マスク編集モード」と現在のツール・ブラシサイズを表示する。
- 画像詳細ビューアの下部情報と「選択切替」「この画像からブランチング」は画像外の通常フローに置き、縦長画像や画像読み込み失敗時でもマスクキャンバスに重ねない。
- マスク編集ツールバー内の入力欄にフォーカスがある間は、画像詳細ビューのショートカットを発火させない。Positive prompt では Space や Ctrl+A などの標準入力操作を優先する。
- マスク編集ツールバーの Positive prompt 直下にあるバッチサイズスライダーは、通常の生成フォームの `batchSize` draft と同期し、マスク付き img2img 送信時にも同じ値を使う。
- 画像詳細ビューアは通常表示でもホイールでズームし、左ドラッグまたは中ドラッグで画像を移動できる。マスク編集中の左ドラッグはマスク描画に使うため、画像移動は中ドラッグで行う。

## 逐次生成・停止

- 通常生成の `batchSize` は ComfyUI 内部の大きな1バッチではなく、GURUGURU 側で `batch_size=1` の `generation_jobs` に分解してキュー投入します。
- 各ジョブは同じ ComfyUI `client_id` を使い、サーバー側 WebSocket で `executed` / `execution_success` / `execution_interrupted` / `execution_error` を監視します。WebSocket通知を取り逃がした場合も、UIの自動 `collect` polling で回収します。
- UIの自動 `collect` polling は3秒間隔です。サーバー側 WebSocket 監視が先に画像を取り込んだ場合も、`collect` 応答の round 集計数を見てUIを再読込します。
- 分割ジョブの seed は round に保存した先頭 seed を基準に、`batchIndex` ごとに `seed + batchIndex` を使います。asset の seed は各ジョブの実 seed です。
- 停止ボタンは未実行・待機中 prompt を ComfyUI queue から削除し、実行中 prompt がこの round のものと判断できる場合は ComfyUI `/interrupt` を呼びます。保存済み asset はそのまま残り、停止後も選択や画像からのブランチングに使えます。
- 複数アプリや別ユーザーが同じ ComfyUI を共有する運用では、`/interrupt` が現在実行中の ComfyUI ワークフローへ作用する点に注意します。

## Workflow diagram

- WorkflowTemplate 一覧の `diagram` ボタンは、保存済みの ComfyUI API形式 workflow JSON から Mermaid の簡略図を表示します。
- テンプレート登録は通常時ボタンだけを表示し、押すとモーダルで API形式 workflow JSON、role map、Mermaid プレビューを確認して登録します。
- Mermaid プレビューは workflow JSON の `inputs` にある `[node_id, output_index]` 形式の接続をエッジとして扱います。role map で参照されるノードは図上で強調表示します。

## イテレーションツリーのスクロール保持

- イテレーションツリー (`.iteration-tracker`) は `render()` で `app.innerHTML` ごと再生成されるため、DOM 再生成前後で `scrollLeft` / `scrollTop` を保存・復元する必要があります。
- `render()` はデフォルトでスクロール位置を保存・復元します。Round 選択 (`selectRound`) や画像詳細オープン (`openAssetDetail`) でも `render()` をそのまま使い、スクロール保持を有効にします。
- `render({ preserveIterationScroll: false })` はスクロールを意図的にリセットしたい場合だけ使います。
- プロジェクト切替 (`openProject`) や Home 戻り (`loadHome`) では、別プロジェクトの古いスクロール位置を引き継がないよう `state.iterationScroll = null` を明示的にリセットします。
- `scrollIntoView()` で active ノードに寄せる修正は、ユーザーの手動スクロール位置を維持する要件と逆になるため入れません。

## ドキュメント運用

- 新しい操作手順、注意点、調査で分かった落とし穴はこのファイルに追記します。
- リポジトリに関する Markdown を更新したときは、変更履歴にも要点を追記します。
- 古い大きな仕様書を復活させるのではなく、実際に役立つ運用メモとして短く保ちます。

## 変更履歴

- 2026-07-03: PoseControlNet フェーズ5〜6（サーバ添付パイプライン + バッジ、最終フェーズ）。`ControlNetOptions` 型（`shared/types.ts`）、`src/server/workflowControlNet.ts` の `patchControlNetPath`（`ControlNetApplyAdvanced` の `inputs.image` 接続トレース + LoadImage 差替え + strength/start/end 注入）、`rounds.ts` の `prepareControlNetRequest`（`decodeControlImageDataUrl`/`storeControlImage`/`request_json` 保存前 null 化）、`workflowRoleMap.ts` への `controlnet_apply_node`/`controlnet_strength_input` 等の推論追加、client `controlnetRequestForParent`（`ControlNetApplyAdvanced` 有無で capability 判定）、`iconPose`/`.pose-badge` グリッドバッジを実装。`workflow.test.ts` に characterization test 4件（apply node 特定・LoadImage 新規追加・`controlnet: null` no-op・ControlNetApplyAdvanced 不在テンプレートでの optional no-op、load_image_input 誤推論との衝突で pose が勝つケース含む）を追加。マージ後 main: typecheck 0 / **253 pass** / build / check 成功。ブラウザ smoke（test DB、port 3000）: pose タブ・スケルトンオーバーレイ・pose パネル（モデルカード/検出/添付トグル/strength・start・end スライダー）が既存 mask タブと相互排他したまま正常表示、console/server error なし。これで PoseControlNet 機能（フェーズ1〜6）は完了。
- 2026-07-02: 新機能実装を開始（ツリー配色改善とポーズControlNetフェーズ1をmainへマージ済み）。使用量上限のため中断し、再開手順と中断中worktree（`fix/mask-pen-lag`・`feature/mask-feather`）の状態を `Docs/Progress-NewFeatures.md` に記録。
- 2026-07-02: ポーズControlNet機能フェーズ1（モデル配布）。`src/shared/constants.ts` に `DEFAULT_POSE_MODEL_BASE_URL`（`/api/pose-models`）と `GITHUB_POSE_RELEASE_API_URL`（GitHub Release `pose-models-v1`）を追加。`src/server/index.ts` の WebSAM 専用 asset proxy（`webSamReleaseAssetNames: Set` + `serveWebSamReleaseAsset`）を「ファイル名 → release API URL」の `Map` レジストリ（`releaseAssetRegistry`）+ 汎用ハンドラ `serveReleaseAsset(res, filename, label)` に一般化。既存 `GET /api/websam-models/:filename` の status code・error message・header・streaming・cache-control は完全維持（`label="WebSAM"` でメッセージ文言も同一）。新ルート `GET /api/pose-models/:filename` を追加し `pose_landmarker_full.task` のみ許可。GitHub token 要件（`GURUGURU_GITHUB_TOKEN`/`GH_TOKEN`/`GITHUB_TOKEN`、無ければ503）は共通。クライアント側（pose worker/UI）は未着手（フェーズ2以降）。typecheck/test/build/check/`git diff --check` 成功、test server (PORT=5544, 外部 `GURUGURU_TEST_DATA_DIR`) で `/api/websam-models/slimsam-77-encoder.onnx`（200）・`/api/pose-models/pose_landmarker_full.task`（200, content-length 9398198 一致）・`/api/pose-models/unknown.bin`（404）を curl 確認。
- 2026-07-02: `Docs/` フォルダを新設。`引継ぎ資料.md` を `Docs/Done/RefactoringLog2026-07-02.md` へ改名移動、参照用 ComfyUI ワークフロー置き場 `Docs/ReferenceFlows/`（`ComfyUI_00147_controlnet.json`）を追加。新機能の設計ドキュメント5件（ポーズControlNet・マスクfeather・ペイントツール・マスクペンかくつき修正・ツリー配色改善）と索引 `Docs/README.md` を起票。
- 2026-07-02: マスク編集をONにして画像詳細ビューアを出入りしてもマスクON状態を維持するよう変更。グリッド画像のMASKバッジ（緑↔灰色）をクリックしてタグ表示ON/OFF切り替え可能にした（`state.showMaskGridTag`、`toggle-mask-grid-tag` action）。
- 2026-07-02: 画像グリッドのシード値表示（`.seed-chip`）をクリックするとクリップボードにコピーし、一時的に「copied」と表示される機能を追加（`copy-seed` action）。
- 2026-07-02: リファクタリング第13フェーズ（P2: 型境界の整備）。API row/response 型（`ProjectSummary`・`Round`・`Asset`・`AssetParent`・`ProjectDetail`・`CollectRoundResponse`・`ComfyStatus`・`WorkflowTemplate`）を `src/shared/apiTypes.ts` に集約。client `main.ts` のローカル interface 9個と `ComfySettings`/緩い `GenerationRequest` の重複を削除して shared に統一（`workflowTypes.ts` は re-export 維持）。server handler の `toApiRow`/`toApiRows` 直後を単一の `as unknown as T` cast で共有型に狭めた（`decorateAsset(): Asset`・`listProjects(): ProjectSummary[]`・`getProjectDetail(): ProjectDetail` 等）。runtime 挙動・response shape は不変。`createProject`/`getProjectDetail` の project row に `roundCount`/`assetCount` が付かない既知の不整合は `projects.ts` の NOTE コメントに記録。typecheck/build/check/`git diff --check` 成功、test server (PORT=5399) で API smoke 実施。これで計画リファクタリング（P0〜P2）は完了。
- 2026-07-02: リファクタリング第12フェーズ。TypeScript strict typecheck を導入。`typescript`/`@types/node` を devDependency に追加し、`tsconfig.json`（strict, noEmit）と `npm run typecheck` を新設。既存の型エラー26件を挙動変更なしで解消（timer 型の `number` 化、`hasActiveMaskData` の type predicate 化、WebSAM worker の `InferenceSession` instance 型修正、`node:sqlite` の `SQLInputValue` 対応など）。`any`/`@ts-ignore`/tsconfig 弱体化は不使用。
- 2026-07-02: リファクタリング第11フェーズ。client `main.ts` のマスク編集 / WebSAM / canvas layer 周辺の型・下書き helper・canvas helper を分割。型（`InpaintDraft`・`SamMaskCandidate`・`MaskStrokeKind`・`ActiveMaskStroke`・`ActiveBoxPrompt`・`ActiveImagePan`・`MaskLayerSet`・`MaskBrushCursorKind`）は `src/client/maskTypes.ts`、下書き helper（`maskedContentOptions`・`defaultInpaintDraft`・`normalizeInpaintDraft`・`hasMaskData`・`hasActiveMaskData`・`isMaskedContent`）は `src/client/maskDraft.ts`、canvas / 幾何 / layer 合成 helper（`createLayerCanvas`・`createMaskLayerSet`・`clearCanvas`・`drawDataUrlIntoCanvas`・`renderFinalMaskToCanvas`・`composeFinalMaskDataUrl`・`maskLayerForStroke`・`paintStroke`・`canvasHasMaskPixels`・`pointerToMaskCanvasPoint`・`normalizePromptBox`・`sampleBrushPromptPoints`・`distanceToSegmentSq`）は `src/client/maskCanvas.ts` に抽出。`main.ts` は stateful orchestration（`state`・event binding・`maskLayerCache`・`webSamWorker` lifecycle・`commitMaskLayers`・`drawMaskSegment`・`ensureMaskLayerSet` / `getOrCreateMaskLayerSet` 等）中心に縮小。`BRUSH_PROMPT_POINT_SPACING` / `BRUSH_PROMPT_MAX_POINTS`（48/48）は `main.ts` 側に維持。新 module は `main.ts` を逆 import しない（circular import なし）。最終マスク合成 `finalMask = (samMask OR manualInclude) AND NOT manualErase`・preview SAM mask 表示・`composeFinalMaskDataUrl(..., false)`・手動ペンの `manualInclude` + `manualErase` `destination-out`・消しゴムの `manualErase`・白 `strokeStyle`/`fillStyle`・`lineCap`/`lineJoin` round・`maskedContent` 既定 `"original"`・`brushSize` 既定 `48`・`maskOpacity` 既定 `0.58`・旧 `maskDataUrl` fallback はすべて維持。UI/API/DB/生成/マスク合成挙動は変更なし。Playwright (1680x920, PORT=5277, GURUGURU_TEST_DB=1, 外部 `GURUGURU_TEST_DATA_DIR`) で Home→Project 作成→source asset upload→asset detail→マスク編集 ON→手動ブラシ描画→適用→消しゴム切替→Smart selection タブ切替の browser smoke を実施、app.js 由来の console error / page error なし（console error は ONNX Runtime wasm の既存 performance 警告のみ）。`GURUGURU_TEST_DB=1 npm run build` / `npm run check` / `git diff --check` 成功。
- 2026-07-02: リファクタリング第10フェーズ。client `main.ts` の workflow/template/import/diagram/defaults 周辺を分割。型は `src/client/workflowTypes.ts`、workflow import / template export の純粋 helper は `src/client/workflowImport.ts`、WorkflowTemplate から生成フォーム既定値を読む helper は `src/client/workflowDefaults.ts`、Mermaid 初期化・workflow diagram / template 周辺 UI renderer は `src/client/workflowUi.ts` に抽出。`main.ts` は stateful orchestration 中心に縮小。`loadWorkflowFile` の DOM/state 更新は `main.ts` 残し、parse 判定ロジックと既存メッセージは pure helper に寄せた。Mermaid 設定値・stale render 防止・selector・`data-action`・HTML 構造・文言・API/DB/生成挙動は維持。Playwright (1680x920) でテンプレート登録〜diagram modal まで browser smoke 実施、JS error なし。
- 2026-07-02: リファクタリング第9フェーズ。round / generation lifecycle 一式を `src/server/index.ts` から `src/server/rounds.ts` に抽出。`createGenerationRound`・`collectRound`・`interruptRound`・`deleteRoundTree`・`ensureRoundMonitor` を public API とし、round monitor の in-memory state (`activeRoundMonitors`・`roundCollectionLocks`・各 status Set) は `rounds.ts` の module-level で保持。`errorToJson`・`stopRoundMonitor`・`handleComfySocketMessage`・`collectRoundUnlocked`・`getRoundForApi` 等は private helper 化。`server/index.ts` は起動・`routeApi()` dispatch・settings・WebSAM model proxy・shutdown のみに縮小。`getProjectDetail(id, { ensureRoundMonitor })` の callback 注入は維持。route dispatch、status code、error message、response shape は維持。

- 2026-07-02: リファクタリング第8フェーズ。source asset upload handler と周辺 helper を `src/server/index.ts` から抽出。`createSourceAsset` → `src/server/sourceAssets.ts`、`normalizeGenerationRequest`/`clampInteger`/`maxBatchSize` → `src/server/generationRequest.ts`、`decodeImageDataUrl`/`decodeMaskDataUrl`/`normalizedUploadFileName` → `src/server/uploadDataUrl.ts`、`nextRoundIndex`/`branchAssignmentForRound` → `src/server/roundBranches.ts`。route dispatch、status code、error message、response shape は維持。
- 2026-07-02: リファクタリング第7フェーズ。project API handler を `src/server/projects.ts` に抽出し、`listProjects`・`createProject`・`deleteProject`・`getProjectDetail` を `server/index.ts` から移動。`getProjectDetail` の round monitor 再開は `ensureRoundMonitor` callback 注入で維持し、route dispatch、status code、error message、response shape は維持。
- 2026-07-02: リファクタリング第6フェーズ。asset API handler/helper を `src/server/assets.ts` に抽出し、`updateAssetStatus`・`serveAssetFile`・`decorateAsset`・asset status 変更用 `selectionActionFor` を `server/index.ts` から移動。route dispatch、status code、error message、response shape は維持。
- 2026-07-02: `nodeIdFromRolePath` を `src/shared/workflowRolePath.ts` に集約し、`server/index.ts`・`server/workflow.ts`・`shared/workflowDiagram.ts` の role map path 先頭 node 抽出を共有化。`normalizeMaskedContent` の server fallback を `original` に揃え、引継ぎ資料の第1フェーズのみを現在状態のように読ませる記述を整理。
- 2026-07-02: リファクタリング第4フェーズ。重複 helper の共有化と server/client の純粋 helper・handler の追加抽出を独立 commit で実施。`isJsonObject`/`Json` 型を `src/shared/json.ts` に集約し `server/validate.ts`・`client/json.ts`・`shared/workflowRoleMap.ts`・`shared/workflowDiagram.ts` の重複を廃止。server 側 template handler (`listTemplates`/`createTemplate`/`deleteTemplate` + `parseJsonInput`) → `src/server/templates.ts`、ファイル配信 helper (`serveStatic`/`streamFile`/`contentTypeFor` + `publicDir`) → `src/server/files.ts`、client 側 API wrapper (`api()`) → `src/client/api.ts`。いずれも verbatim 抽出。
- 2026-07-02: `serveStatic` が実在するディレクトリ URL (`/ort/` 等) で `createReadStream(ディレクトリ)` を起こし、レスポンスを返さずハングする既存バグを修正。ディレクトリは配信対象外として SPA fallback に回した（`isServableFile` で regular file のみ配信）。ファイル配信・traversal 保護・asset 配信は維持。
- 2026-07-02: リファクタリング第3フェーズ。挙動変更なしの純粋 helper 抽出を独立 commit で実施。server 側: HTTP utility (`sendJson`/`readJson`/`HttpError`) → `src/server/http.ts`、検証/coercion helper (`objectBody`/`requiredString`/`stringOr`/`numberOr` 等) → `src/server/validate.ts`。client 側: format/escape helper (`escapeHtml`/`escapeAttr`/`formatDate`/`formatNumber`/`formatCssNumber`/`formatSliderValue`) → `src/client/format.ts`、JSON object helper (`parseJsonObjectText`/`isJsonObject`/`pickJsonObject`) と `Json` 型 → `src/client/json.ts`。関数本文は `export` 追加のみで一致、UI/API/DB/保存形式には非接触。

- 2026-07-02: generation mode ごとの denoise 既定値・正規化、parent asset 要否、parent relation 変換を `src/shared/generationMode.ts` に集約。`client/main.ts` と `server/index.ts` の重複 helper を共有 import に切り替えた。
- 2026-07-01: ファイルパス境界判定を `src/server/paths.ts` に集約。`isPathInside`（厳密: 子のみ）と `isPathInsideOrEqual`（同値含む）を用意し、`storage.ts` の `safeFileStream`、`storeMaskImage`、`deleteProjectStorage`、`index.ts` の `serveStatic`/`streamFile`、`db.ts` の起動時 data dir 拒否を prefix `startsWith` 判定から `relative()` ベースへ寄せた。prefix 類似パス・大小文字・cross-drive の誤判定を防ぐ。挙動は維持（同値ケースは意図通り分離）。
- 2026-07-01: リファクタリング調査結果、第1フェーズ実施内容、次フェーズ候補をまとめた `引継ぎ資料.md` を追加。
- 2026-07-01: UI レイアウト検証では横長 viewport を明示確認し、検証後に viewport override を戻す注意を `AGENTS.md` にも追記。
- 2026-07-01: ComfyUI workflow JSON から Mermaid 簡略図を生成し、WorkflowTemplate 一覧の `diagram` ボタンとテンプレート登録モーダル内プレビューで表示するようにした。
- 2026-07-01: 消しゴムで消した領域を後からペンで再描きして復活できるよう、`drawMaskSegment` の `manual-include` で `manualErase` レイヤーを同形状 `destination-out` で削除するように修正。ストローク描画を `paintStroke` ヘルパに共通化。
- 2026-07-01: マスク編集時にブラシサイズ相当の半透明円（`.brush-cursor`）を画像座標系で追従表示。ペン/消しゴム/Brush prompt で色分け、Point/Box モードでは非表示。ズーム・パンでもズレない。
- 2026-07-01: WebSAM Brush prompt のサンプリングを `BRUSH_PROMPT_POINT_SPACING=48` / `BRUSH_PROMPT_MAX_POINTS=48` に間引き、定数化。decode が過剰な点で重くなるのを抑制。
- 2026-07-01: 手動マスク inpaint の `maskedContent` 既定を `original` に変更し、低デノイズでマスク部が灰色ベタで残るのを防いだ。paste back 用 `ImageCompositeMasked` のマスクを `GrowMask` 済みではなく元のマスクに分離し、padding 領域へ生成結果が広く貼り戻されるのを防ぐ。選択肢ラベルで灰色化の有無を明示。
- 2026-07-01: デノイズ強度をラウンド切替・親画像切替で保持するよう修正。`selectRound` は `denoise` だけ引き継ぎ、`fillGenerationFormFromAsset` は既存 denoise を上書きしない。表示値を `normalizeDenoiseForMode` で正規化し送信値と一致させる。
- 2026-07-01: 画像詳細ビューアの通常表示にホイールズームと左/中ドラッグ移動を追加し、マスク編集中は中ドラッグで画像移動できるようにした。
- 2026-07-01: UI レイアウト検証時は一時的にワイド表示へ切り替え、確認後に戻す手順を追記。
- 2026-07-01: マスク編集に WebSAM / SlimSAM-77 のスマート選択UI、ONNX Runtime Web Worker、GitHub ReleasesモデルURL設定、OPFSキャッシュ、Point/Box/Brush prompt、候補マスク適用、手動修正との合成、ズーム表示を追加。
- 2026-07-01: SlimSAM-77 の ONNX モデルを GitHub Release `websam-models-v1` に配置し、private repo の release asset を読むための `/api/websam-models` proxy を WebSAM の既定モデルURLとして自動設定するようにした。
- 2026-07-01: inpaintでも指定サイズへ親画像とマスクを同一変換するようにし、サイズ欄に縦横比固定の拡大・縮小ボタンを追加。
- 2026-07-01: MASK 表示を「次回ブランチングへ有効マスクとして入る画像」だけに限定し、マスク付き生成の子画像や OFF 状態の下書きマスクは通常画像として扱うようにした。
- 2026-07-01: 通常img2imgで指定した幅・高さが効くよう、親画像を `ImageScale` でリサイズしてから `VAEEncode` する注意を追加。
- 2026-07-01: 自動 `collect` polling を3秒間隔にし、サーバー側 WebSocket 監視が先に取り込んだ生成画像もUIへ逐次反映するようにした。
- 2026-07-01: バッチ生成を1枚単位の `generation_jobs` に分解し、ComfyUI WebSocket監視・逐次取り込み・停止ボタン・停止後ブランチングを追加。
- 2026-07-01: マスク編集ツールバーの Positive prompt 入力中に Space が画像選択ショートカットへ奪われる問題を修正。
- 2026-07-01: マスク編集ツールバーの Positive prompt 直下にバッチサイズスライダーを追加し、通常生成フォームのバッチサイズと同期するようにした。
- 2026-07-01: マスク編集 ON/OFF ボタンを画像詳細ビューア上部の画像外コントロールバーへ移し、マスクキャンバス上の切り替え文字が描画ドラッグを奪わないようにした。
- 2026-07-01: 画像詳細ビューア下部フッターを絶対配置から通常フローへ変更し、縦長画像や画像なし状態で下部ボタンが伸びたりマスクキャンバスへ重なったりしないようにした。
- 2026-07-01: 画像詳細ビューア左上にマスク編集 ON/OFF ボタンを追加し、マスク編集中のモード表示を追加。最小化ツールバーがマスク描画ドラッグを奪わないようにした。
- 2026-07-01: マスク編集モードに入った直後、ツールバーを自動最小化するようにした。縦長画像で展開済みツールバーが画像全体を覆い、プロンプト等の文字要素が選択されてしまう問題を修正。
- 2026-07-01: 画像詳細ビューアのプロンプト・マスク設定ダイアログを最小化・移動可能にし、ダイアログ表示中はマスクを無効化、ダイアログ外タップで最小化する挙動を追加。
- 2026-07-01: イテレーションツリーの Round クリック時のスクロールジャンプを修正。`render` をオプションオブジェクト化し、`selectRound` / `openAssetDetail` を `render()` に変更。プロジェクト切替・Home 戻りで `state.iterationScroll` をリセット。
- 2026-07-01: inpaint マスクを ComfyUI に渡す際、alpha 反転を避けるため `red` チャンネルを使う注意を追加。
- 2026-07-01: 画像詳細ビューアのマスク編集と img2img inpaint 用の保存・検証方針を追記。
- 2026-07-01: Codex 検証用インスタンスは既定ポート `5177` を避け、テストDBと外部テストデータディレクトリを使う注意を追加。
- 2026-07-01: 旧仕様書を削除し、操作方法と注意点を残すための `操作メモ.md` を作成。`AGENTS.md` に今後の追記先と git commit 方針を追加。
