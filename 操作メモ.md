# 操作メモ

GURUGURU の操作方法や注意点を追記するためのメモです。大きな仕様書ではなく、実際に開発・運用するときに必要な手順と落とし穴を残します。

## 開発時の基本操作

- 依存関係は `npm install` で用意します。
- ビルドは `npm run build` を使います。
- 開発起動は `npm run dev` を使います。これはビルド後に `dist/server/index.js` を起動します。
- 構文チェックを含む確認は `npm run check` を使います。
- 型チェックは `npm run typecheck`（`tsc --noEmit`、strict）を使います。コード変更後はゼロエラーを維持します。
- 実行には Node.js 24 以上が必要です。
- CSS のブラウザ基準は **Chrome 150** を前提とします。`:has()`・`mask` 合成・CSS anchor positioning などの新しい CSS 機能をフォールバックなしで使ってよく、後方互換のための prefix / 代替実装は不要です（`src/client/styles.css` 冒頭コメントにも明記）。

## データ保存場所

- 通常実行時のユーザーデータは OS のユーザーデータディレクトリに保存されます。Windows では `%LOCALAPPDATA%\GURUGURU` が既定です。
- `GURUGURU_DATA_DIR` を指定する場合は、必ずこのリポジトリ外のパスにします。
- `app.db`、生成画像、プロンプト、ワークフロースナップショット、サムネイルなどのランタイムデータはリポジトリに置きません。
- リポジトリ直下や `data/` に実運用データベースを作らないでください。

## テスト・検証時の注意

- データベースを初期化するコマンドやテストでは `GURUGURU_TEST_DB=1` を設定します。
- テスト用データの保存先を明示したい場合は `GURUGURU_TEST_DATA_DIR` を使います。
- 本番のユーザーデータディレクトリや実運用の `app.db` を、テスト・調査・要約目的で読み込まないでください。
- コマンドがリポジトリ内のデータディレクトリを拒否した場合は、外部のテスト用ディレクトリを指定してやり直します。
- 既定ポート `5177` はユーザーが `npm start` などで起動する通常インスタンス用に空けておきます。エージェントが検証用にアプリを起動する場合は `PORT` に 5177 以外を指定し、`GURUGURU_TEST_DB=1` とリポジトリ外の `GURUGURU_TEST_DATA_DIR` を使います。
- 複数インスタンスを同時に動かす場合は、ポートだけでなくデータディレクトリも分けます。同じ本番データディレクトリを複数プロセスから使う運用は避けます。
- UI レイアウト検証では、サイドバーやモーダルの崩れを確認するためにブラウザ viewport を一時的にワイド表示（例: 1680x920）へ切り替えてよいです。確認後は viewport 設定を戻します。
- `curl` でサーバー疎通確認するときは必ず `--max-time 5` を付ける。dev server・watch・test・npm script などハングしうるコマンドは長時間待たず、1つの確認が 5〜10秒以上返らない場合は中断して原因を報告する。サーバー疎通確認では全 URL が必ずレスポンスを返すことを確認する（ディレクトリ URL でハングしないこと）。
- 本番 ComfyUI（既定ポート 8188）も本番ユーザーデータとして扱う。生成内容が見えるエンドポイント — `/history`（ワークフロー・プロンプト・シード）、`/view`（画像）— は読まず、input/output ディレクトリも閲覧しない。疎通確認レベル（`/queue`・`/system_stats`・`/object_info`）は可。エラー診断は、UI に表示されるエラーメッセージの共有をユーザーに依頼するか、下記のテスト用 ComfyUI インスタンスで再現して行う。本番インスタンスへの状態変更（`/free`・`/interrupt` 等）は事前にユーザーへ確認する。

## テスト用 ComfyUI インスタンス（DL 済み Desktop 版の流用）

生成パイプライン（workflow patch・ControlNet・統合 switch 等）の実機検証は、本番 ComfyUI（8188）ではなく、ダウンロード済み Desktop 版 ComfyUI のコードと Python 環境を流用した分離インスタンス（port 8288）で行います。実績: 2026-07-03 の UnifiedSwitch ControlNet 検証。

- コード: `$env:LOCALAPPDATA\Comfy-Desktop\ComfyUI-Installs\ComfyUI\ComfyUI`
- Python: 同ディレクトリの `standalone-env\python.exe`
- モデルは `%APPDATA%\Comfy Desktop\shared_model_paths.yaml` 経由で Desktop 版と共有（ダウンロード不要）

```powershell
# 1. リポジトリ外にテスト用 base ディレクトリを用意（custom_nodes が無いと起動時 FileNotFoundError）
$base = "$env:TEMP\comfy-test-8288"
New-Item -ItemType Directory -Force "$base\custom_nodes", "$base\input", "$base\output", "$base\temp"
# 統合 switch 検証では未使用枝の LoadImage にも実在ファイルが必要 → input にダミー画像を置く

# 2. 起動
$comfy = "$env:LOCALAPPDATA\Comfy-Desktop\ComfyUI-Installs\ComfyUI"
& "$comfy\standalone-env\python.exe" "$comfy\ComfyUI\main.py" `
  --port 8288 --base-directory $base `
  --extra-model-paths-config "$env:APPDATA\Comfy Desktop\shared_model_paths.yaml"

# 3. 疎通確認
curl --max-time 5 http://127.0.0.1:8288/system_stats
```

- GURUGURU 側はテストインスタンス（`PORT` ≠ 5177・`GURUGURU_TEST_DB=1`・外部 `GURUGURU_TEST_DATA_DIR`）を起動し、設定画面の ComfyUI URL に `http://127.0.0.1:8288` を設定して接続します。
- GPU 負荷を抑えるため、検証は低負荷パラメータ（例: 512x288 / 6 steps）で行います。
- テストインスタンスの output/input はテスト専用なので閲覧・削除して構いません。

## 画像マスク・inpaint

- 通常の img2img は、親画像を指定した幅・高さへ `ImageScale` でリサイズしてから `VAEEncode` に渡す。フォームの幅・高さは EmptyLatent ではなく、このリサイズノードで実際の出力サイズに反映される。
- マスク付き inpaint は、保存時のマスク寸法が親画像と一致していることを検証したうえで、生成時に親画像とマスクを同じ指定幅・高さへリサイズしてから `VAEEncode` / 合成へ渡す。
- 画像詳細ビューアのマスク編集は、親画像と同じサイズの PNG マスクだけを生成リクエストに使います。
- GURUGURU が生成するマスク PNG は透明背景に白で描画します。ComfyUI の `LoadImageMask` は alpha を反転マスクとして読むため、workflow patch では `red` チャンネルを使い、白い描画部分を更新対象にします。
- スマート選択で WebSAM / SlimSAM-77 を使う場合、既定ではローカルAPI `/api/websam-models` 経由で GitHub Release `websam-models-v1` の `slimsam-77-encoder.onnx` と `slimsam-77-decoder.onnx` を取得します。別の配布先を使う場合だけ、設定画面の `WebSAM model base URL` を変更します。
- このリポジトリは private のため、既定の `/api/websam-models` で release asset を読むには GURUGURU サーバー起動時に `GURUGURU_GITHUB_TOKEN`、`GH_TOKEN`、または `GITHUB_TOKEN` を設定します。token はブラウザへ渡さず、サーバー側で GitHub Release API を呼ぶためだけに使います。
- サーバーの release asset proxy（`src/server/index.ts` の `serveReleaseAsset`）は「ファイル名 → release API URL」のレジストリ方式です。WebSAM 用（`slimsam-77-encoder.onnx` / `slimsam-77-decoder.onnx` → `websam-models-v1`）に加え、ポーズ検出モデル用（`pose_landmarker_full.task` → GitHub Release `pose-models-v1`、`GET /api/pose-models/:filename`）を同じハンドラで配信します。token 要件・404/503 の挙動・streaming・cache-control は用途間で共通です。
- SlimSAM-77 のモデルファイルはブラウザの OPFS にキャッシュされます。2回目以降はキャッシュ済みファイルを優先します。OPFS非対応ブラウザでは手動マスク機能はそのまま使えますが、WebSAMのキャッシュは使えません。
- スマート選択の最終マスクは `finalMask = (samMask OR manualIncludeMask) AND NOT manualEraseMask` として合成し、この白黒PNGだけを `maskDataUrl` として送ります。SAM候補プレビューは「適用」するまで `samMask` として確定しません。
- 手動ペン（`manual-include`）で描くときは `manualInclude` レイヤーに加えて同じストローク形状を `manualErase` レイヤーから `destination-out` で削除する。これにより「消しゴムで消した領域を後からペンで再描きして復活させる」ことができる。消しゴムは引き続き `manualErase` に追加し、SAM結果や手動追加を最終合成で抜く。
- WebSAM Brush prompt のサンプリング間隔は `BRUSH_PROMPT_POINT_SPACING`（48px）/ `BRUSH_PROMPT_MAX_POINTS`（48点）で間引く。マジックナンバーではなく定数化し、過剰な点で decode が重くなるのを防ぐ。消しゴムによる点削除（`removeBrushPromptPointsNearSegment`）はブラシ半径ベースなので間引いた後も整合する。
- マスク編集時はブラシサイズ相当の半透明円（`.brush-cursor`）を画像座標系で追従表示する。ペン=緑、消しゴム=赤、Brush prompt=青。Point/Box モードでは非表示。ズーム・パンでも画像座標とズレない（SVG overlay が `mask-zoom-stage` 内で一緒にスケールする）。
- inpaint の `maskedContent` 既定は `original`（元画像の潜在を維持 + `SetLatentNoiseMask`）。`fill`（`VAEEncodeForInpaint` でマスク部をゼロ埋め）は低デノイズで灰色ベタが残りやすいため既定から外した。UIの選択肢ラベルで灰色化の有無を明示している。
- `patchInpaintLatentPath` では、サンプラー用ノイズマスク（`GrowMask` 済み・padding 含む）と、最終 paste back 用 `ImageCompositeMasked` のマスク（元の非拡大マスク）を分離する。これにより padding 領域へ生成結果が意図より広く貼り戻されるのを防ぎ、灰色化を悪化させない。
- デノイズ強度はラウンド切替や親画像切替でも保持する。`selectRound` はフォーム全体を破棄せず `denoise` だけ引き継ぐ。`fillGenerationFormFromAsset` は既存の denoise を上書きしない（txt2img など `requiresFullDenoise` のモードを除く）。`renderGenerationPanel` の表示値は `normalizeDenoiseForMode` で正規化し、実際に送信される `request.denoise` と一致させる。
- `generation_rounds.request_json` には `maskedContent`、`inpaintArea`、`onlyMaskedPadding` と保存済みマスク参照だけを残し、巨大な `maskDataUrl` は保存しません。
- マスク画像は各 Project の外部データディレクトリ配下 `projects/<projectId>/masks/` に保存されます。本番DBや本番データを調査せず、検証時は必ず外部テストデータディレクトリを使ってください。
- 画像カードの `MASK` 表示は、次回の img2img ブランチング request にマスクが実際に入る状態だけに付けます。マスク付き生成から生まれた画像でも、その画像自身に有効なマスク下書きがなければ通常画像として扱います。
- マスク編集を OFF にした場合、描いたマスク画像は下書きとして残しても request には含めず、`MASK` 表示も外します。再度 ON にすると残っている下書きを編集・適用できます。

## 画像貼り付け(Paste & Transform)

- アセット詳細モーダル表示中に、画像を**ドラッグ&ドロップ / Ctrl+V / ペイントパネルの「画像を貼り付け」ボタン / ギャラリーサムネイルのドラッグ**で貼り付けできます(PNG / JPEG / WebP、長辺 4096px へ自動縮小)。マスク編集中・非編集中のドロップは自動でペイント編集+選択ツールへ切り替わります。
- 貼り付けは**元画像を書き換えない「添付」**です。配置(移動・回転・拡縮)はアセット単位でサーバへ自動保存され(`asset_paste_attachments` + `projects/<projectId>/paste_sources/`)、プロジェクトを開き直しても復元されます。**どのツール中でも貼り付け画像のダブルクリックで再選択**できます。
- 操作: 本体ドラッグ=移動(Shift=軸固定)/ 角ハンドル=拡縮(Shift=縦横独立)/ 上ハンドル=回転(Shift=15°スナップ、ダブルクリック=0°)/ 矢印=1px(Shift=10px)/ Delete=削除 / Esc=選択解除。パネルの操作行から複製・前面へ・背面へ・焼き込み(ペイントレイヤーへ確定し以降ブラシで上描き可)・削除。Ctrl+Z はストロークと貼り付け操作を**時系列で統合 undo** します(削除の取り消しも可)。
- img2img 系生成時は**保存操作なしで**「元画像+ペイントレイヤー(未保存ストローク込み)+添付」を見たまま合成し、`request.pasteComposite.imageDataUrl` として送信します。サーバは `projects/<projectId>/composites/<roundId>_composite.png` へ保存して ComfyUI の img2img 入力に使い、`request_json` には `compositePath` と生成時点の `objects` スナップショットだけを残します(dataUrl は保存しない)。**親アセット・ツリー構造は不変**(「エッジに添付」モデル)。
- ツリーのエッジポップアウトに「添付 n件 ˅」が出ているエッジは、ポップアウト上の**ホイール下スクロール(またはエッジクリック)で展開**して使用した貼り付け画像のサムネイルを確認できます。
- 落とし穴: 合成 PNG が 16MB を超えると生成前にクライアント側で中断されます(トースト表示)。`paste_sources/` のファイルは**手動で削除しないでください**(過去ラウンドの `request_json` からも参照されます)。どこからも参照されなくなったソースファイルは**サーバ起動時に自動掃除**されます(`purgeOrphanPasteSources`。削除の瞬間に消さないのは undo とラウンド履歴参照のため)。外部サイトからの画像 URL ドラッグ(ファイル実体なし)は taint 回避のため拒否されます。

## ポーズ ControlNet 添付

- 画像詳細ビューアのマスク編集サイドバーに「マスク / ポーズ」タブがあり、ポーズタブでは検出モデル（MediaPipe Pose Landmarker Full / Heavy、CIGPose）で検出した OpenPose 18点を関節ドラッグ・エッジ選択・マーキーで編集できる（複数人最大4人・Undo・Shift ドラッグの骨長固定 FK 対応）。編集対象は常に OpenPose 18点（MediaPipe 33 landmarks は検出直後に変換して破棄）。モデルは GitHub Release から `/api/pose-models` 経由で取得し OPFS にキャッシュする。
- ポーズタブの添付チェックを ON にした状態で生成すると、`renderPoseSkeletonDataUrl` が黒背景 + OpenPose 標準配色のスケルトン PNG を描画し、`request.controlnet.poseImageDataUrl` として送信する。**送信先テンプレートの workflowJson に `ControlNetApplyAdvanced` ノードが無い場合は添付自体を行わない**（クライアント側 capability チェック、`workflowHasControlNetApply`）。
- サーバーはマスクと同型の添付パイプラインで処理する: `decodeControlImageDataUrl`（PNG限定・8MB上限）→ `storeControlImage`（`projects/<projectId>/control/<roundId>_pose.png`）→ `uploadImageToComfy` → `patchControlNetPath`。`request_json` へ保存する前に `poseImageDataUrl` は null 化し、巨大 dataURL を DB に残さない（マスクの `maskDataUrl` null 化と同じ規約）。
- `patchControlNetPath` は `ControlNetApplyAdvanced` ノードを `roleMap.controlnet_apply_node`（無ければクラス名検索）で特定し、その `inputs.image` の**接続を辿って**供給元 LoadImage ノードを見つける（class 検索ではなく connection トレース）。辿り着いた LoadImage が親画像用として使われている場合は上書きせず新規 LoadImage を追加する。ControlNet 非対応テンプレートでは何もせず optional に振る舞う。
- **img2img / inpaint と pose 添付は併用できる**（2026-07-03 対応済み、`Docs/Done/Feature-PoseControlNet-Img2Img.md`）。かつて禁止していた原因（`inferRoleMap` の `vae_encode_image_input` 誤推論でワークフローが壊れる）は根本修正され、保存済みテンプレートも生成時の `sanitizeRoleMap` で防御されるため再登録不要。pose 未添付の img2img では ControlNet strength を 0 にして no-op 化する。
- グリッドの `POSE` バッジ（紫、`.pose-badge`）は MASK バッジと同じ「次回生成に添付される」draft 駆動のセマンティクスで、`showMaskGridTag` トグルに乗せている。

## プレビューダイアログの最小化・移動

- 画像詳細ビューアの `.mask-toolbar`（プロンプト・マスク設定）は邪魔にならないよう最小化・移動できる。
- `state.maskToolbarMinimized` が true のとき本体を隠し、ヘッダーと展開ボタンだけを残す。
- ダイアログ表示中（非最小化）は `.mask-canvas` に `mask-locked` を付け `pointer-events: none` にし、マスク描画を無効化する。最小化するとマスクが効く。
- マスク編集モードに入った直後はツールバーを自動で最小化する。縦長画像では展開済みツールバーが画像全体を覆い、プロンプト等の文字要素が選択されてしまうため。プロンプト・設定を編集したいときは展開ボタンで戻す。
- ダイアログ外（画像・背景など）のタップで最小化する。ただし `[data-action]` や `.mask-toolbar` 内のタップは最小化しない。最小化済みのとき背景タップは従来通りモーダルを閉じる。
- ヘッダー (`[data-mask-toolbar-handle]`) のドラッグで `position: fixed` に切り替えて移動し、`state.maskToolbarPos` に保存する。`openAssetDetail` / `closeAssetDetail` / `toggleMaskEditor` で位置と最小化状態をリセットする。
- マスク編集の ON/OFF は、画像詳細ビューア上部の画像外コントロールバーにある常設ボタンで切り替える。マスクキャンバス上に切り替え文字を重ねない。
- 最小化中のツールバー見出しはドラッグ対象にせず、展開ボタン以外はポインターを透過させてマスク描画を妨げない。
- マスク編集中は画像上部に「マスク編集モード」と現在のツール・ブラシサイズを表示する。
- 画像詳細ビューアの下部情報と「選択切替」「この画像からブランチング」は画像外の通常フローに置き、縦長画像や画像読み込み失敗時でもマスクキャンバスに重ねない。
- マスク編集ツールバー内の入力欄にフォーカスがある間は、画像詳細ビューのショートカットを発火させない。Positive prompt では Space や Ctrl+A などの標準入力操作を優先する。
- マスク編集ツールバーの Positive prompt 直下にあるバッチサイズスライダーは、通常の生成フォームの `batchSize` draft と同期し、マスク付き img2img 送信時にも同じ値を使う。
- 画像詳細ビューアは通常表示でもホイールでズームし、左ドラッグまたは中ドラッグで画像を移動できる。マスク編集中の左ドラッグはマスク描画に使うため、画像移動は中ドラッグで行う。

## 逐次生成・停止

- 通常生成の `batchSize` は ComfyUI 内部の大きな1バッチではなく、GURUGURU 側で `batch_size=1` の `generation_jobs` に分解してキュー投入します。
- 各ジョブは同じ ComfyUI `client_id` を使い、サーバー側 WebSocket で `executed` / `execution_success` / `execution_interrupted` / `execution_error` を監視します。WebSocket通知を取り逃がした場合も、UIの自動 `collect` polling で回収します。
- UIの自動 `collect` polling は3秒間隔です。サーバー側 WebSocket 監視が先に画像を取り込んだ場合も、`collect` 応答の round 集計数を見てUIを再読込します。
- 分割ジョブの seed は round に保存した先頭 seed を基準に、`batchIndex` ごとに `seed + batchIndex` を使います。asset の seed は各ジョブの実 seed です。
- 停止ボタンは未実行・待機中 prompt を ComfyUI queue から削除し、実行中 prompt がこの round のものと判断できる場合は ComfyUI `/interrupt` を呼びます。保存済み asset はそのまま残り、停止後も選択や画像からのブランチングに使えます。
- 複数アプリや別ユーザーが同じ ComfyUI を共有する運用では、`/interrupt` が現在実行中の ComfyUI ワークフローへ作用する点に注意します。

## Workflow diagram

- WorkflowTemplate 一覧の `diagram` ボタンは、保存済みの ComfyUI API形式 workflow JSON から Mermaid の簡略図を表示します。
- テンプレート登録は通常時ボタンだけを表示し、押すとモーダルで API形式 workflow JSON、role map、Mermaid プレビューを確認して登録します。
- Mermaid プレビューは workflow JSON の `inputs` にある `[node_id, output_index]` 形式の接続をエッジとして扱います。role map で参照されるノードは図上で強調表示します。

## イテレーションツリーのスクロール保持

- イテレーションツリー (`.iteration-tracker`) は `render()` で `app.innerHTML` ごと再生成されるため、DOM 再生成前後で `scrollLeft` / `scrollTop` を保存・復元する必要があります。
- `render()` はデフォルトでスクロール位置を保存・復元します。Round 選択 (`selectRound`) や画像詳細オープン (`openAssetDetail`) でも `render()` をそのまま使い、スクロール保持を有効にします。
- `render({ preserveIterationScroll: false })` はスクロールを意図的にリセットしたい場合だけ使います。
- プロジェクト切替 (`openProject`) や Home 戻り (`loadHome`) では、別プロジェクトの古いスクロール位置を引き継がないよう `state.iterationScroll = null` を明示的にリセットします。
- `scrollIntoView()` で active ノードに寄せる修正は、ユーザーの手動スクロール位置を維持する要件と逆になるため入れません。

## ドキュメント運用

- 新しい操作手順、注意点、調査で分かった落とし穴はこのファイルに追記します。
- リポジトリに関する Markdown を更新したときは、変更履歴にも要点を追記します。
- 古い大きな仕様書を復活させるのではなく、実際に役立つ運用メモとして短く保ちます。
- 変更履歴は直近分のみ詳細に残し、古い分は年月単位の要約へ畳んで肥大化を防ぎます。

## 変更履歴

- 2026-07-06: 画像貼り付けのフィードバック一括対応。(1) グリッドタイルに貼り付け画像を合成表示し `PASTE` タグで ON/OFF(プロジェクト詳細 API に `pasteAttachments` を同梱、`.paste-grid-canvas` は `<img>` と同じ intrinsic size + `object-fit: cover` でクロップ一致)。(2) エッジポップアウトは本体クリックで閉じない(展開トグルはフッタのみ、外クリック=focus 喪失で閉じる。focus 中は hover が外れても折りたたまない)。(3) 添付サムネイルの展開を `grid-template-rows: 0fr⇄1fr` の transition でアニメーション化。(4) Round ごとに編集内容(プロンプト等)を記憶する `generationDraftsByRound` を追加(ブランチング後に親ノードへ戻っても編集値が残る)。(5) 「JSON初期値」を「ノード元値」に改名し、activeRound の request 値(ノード開始時点)へ戻す仕様に変更(request が無いノードは従来の Workflow JSON 初期値へフォールバック)。(6) 停止済み/失敗 Round のドットが `pending` 扱いで点滅し続ける問題を修正(`interrupted` / `failed` 専用クラス)。(7) 貼り付け画像の「焼き込み」機能を削除(ユーザー指示)。(8) WebSAM のエンコード入力を貼り付け・ペイント込みの見たまま合成に変更。(9) UI ジャンク対策: draft の localStorage 永続化を debounce(400ms、unload で flush)、モデル DL progress の再描画を 150ms スロットル(マスク/ポーズタブ切替が重い・押せない事象の対策)。
- 2026-07-06: テスト用 ComfyUI(8288)の注意: Desktop 版更新後に `standalone-env` から torch 等が消えることがある。`requirements-nvidia.txt`(ピン留め torch)と `ComfyUI/requirements.txt` を `standalone-env\python.exe -m pip install -r` で復元すれば起動できる(2026-07-06 実施)。
- 2026-07-06: 貼り付けソースの起動時 GC(`purgeOrphanPasteSources`)を追加。現在の添付∪全ラウンド `request_json` のどこからも参照されない `paste_sources` のファイル+DB 行をサーバ起動時に削除する(`purgeAllRoundTrash` と同じ起動時パージの流儀。ユーザー提案)。
- 2026-07-06: **画像貼り付け(Paste & Transform)機能を実装**(`Docs/Feature-ImagePaste.md`)。D&D/Ctrl+V/サムネイルドラッグでの貼り付け、SVG ギズモでの移動・回転・拡縮、ダブルクリック再選択、サーバ永続化(開き直しで復元)、生成時の見たまま合成(`pasteComposite`、親ノード不変の「エッジに添付」モデル)、統合 undo(ストローク+貼り付け操作の時系列)、エッジポップアウトのスクロール展開で添付表示。付随修正: ペイント編集中の中ボタンパンが InpaintDraft へ書かれ snap-back するバグを修正。
- 2026-07-05: 第二次リファクタリング完了(フェーズA〜J-5・UX改善#1〜#8・総括コードレビュー修正2件)。「第二次リファクタリング計画.md」を `Docs/Done/` へ移動し `Docs/README.md` を更新。
- 2026-07-05: `Docs/Refactoring/第二次リファクタリング計画.md` を起票（main.ts 再膨張への構造対策・CSS Chrome 150 化・UX 改善候補・テスト用 ComfyUI 手順）。実装済み設計ドキュメント8件を `Docs/Done/` へ移動し `Docs/README.md` を現状に更新。本ファイルの古い記述を整理: img2img × pose 添付の禁止記述を併用対応済み（`Feature-PoseControlNet-Img2Img.md`）に修正、ポーズ検出のモデル選択（Full/Heavy/CIGPose）・添付チェック UI を現状に合わせ、テスト用 ComfyUI インスタンス（port 8288、Desktop 版流用）の起動手順を新設。変更履歴の 2026-07-01〜07-02 分を要約へ畳んだ。
- 2026-07-05: CSS のブラウザ基準を **Chrome 150 前提** と明記（ユーザー指示）。`styles.css` 冒頭コメントと「開発時の基本操作」に追記。イテレーションツリーのエッジを1本のグラデーション曲線にする SVG オーバーレイで `:has()` 等をフォールバックなしで使用。エッジ hover ポップアウトからタイトル行を削除。
- 2026-07-05: イテレーションツリーのエッジ（親→子リンク）に hover ポップアウトを追加。`.iteration-edge-popout` にその Round の生成プロパティ（プロンプト synopsis・解像度・デノイズ・steps・CFG・sampler・scheduler・モード）を表示。トラッカーが `overflow: auto` のため `position: fixed` + `positionIterationEdgePopout`（viewport 基準・下→上フォールバック・端クランプ）で配置。回帰テスト8件追加。
- 2026-07-03: マスク・生成パラメータの localStorage 永続化に `poseDrafts` を追加（ポーズ添付設定がプロジェクト再オープンでリセットされていた問題の修正）。
- 2026-07-03: 本番 ComfyUI（8188）を本番ユーザーデータとして扱う規約を追加（ユーザー指示）。`/history`・`/view` の読み取り禁止、診断はエラー共有依頼またはテスト用インスタンスで。`AGENTS.md` にも追記。
- 2026-07-03: 統合 Switch ワークフローの ControlNet 実行時エラーを修正。Chroma/Flux 系 ControlNet は `ControlNetApplyAdvanced` の `vae` 接続が必須。リファレンス JSON に `vae` 接続を追加し、`patchUnifiedSwitchWorkflow` に vae 未接続テンプレートへの自動復元を実装（インポート済みテンプレートも再インポート不要）。実機検証は port 8288 のテスト用 ComfyUI インスタンスで実施（手順は「テスト用 ComfyUI インスタンス」節）。
- 2026-07-03: `Docs/ReferenceFlows/Reference-UnifiedSwitchWorkflow.{json,md}` を追加。txt2img/img2img/inpaint/ControlNet有無 を `ComfySwitchNode` + `PrimitiveBoolean` で1ワークフローに統合したリファレンス。落とし穴: prompt 検証は lazy に関係なくグラフ全体に走るため、未使用枝の `LoadImage` にも実在ファイル名が必要。
- 2026-07-03: UIフィードバック一括対応6件 — マスク・生成パラメータの localStorage 永続化（`guruguru:draft:<projectId>`）、トースト閉じるボタンのはみ出し修正、Project 名の自動採番（`New Project(2)`…）、サイドバー折りたたみ（`state.sidebarCollapsed`、幅56px）、マスク編集パネルへの生成パラメータ追加（`data-generation-field` で双方向同期）、スポイト後フリーズは Chrome ネイティブカラーピッカーの既知問題と判明しコード変更なし。
- 2026-07-03: PoseControlNet フェーズ5〜6（サーバ添付パイプライン + `POSE` バッジ）で機能完了。コードレビュー指摘により img2img × pose のガード追加と `inferRoleMap` の推論限定も実施（このガードは後日 `Feature-PoseControlNet-Img2Img.md` の根本修正で撤廃済み）。
- 2026-07-02 まとめ（詳細は `Docs/Done/RefactoringLog2026-07-02.md`）:
  - 第一次リファクタリング第6〜13フェーズ完了。server は `assets.ts` / `projects.ts` / `rounds.ts` / `sourceAssets.ts` / `templates.ts` 等へ、client は `maskTypes/maskDraft/maskCanvas` / `workflowTypes/workflowImport/workflowDefaults/workflowUi` へ分割。TypeScript strict `typecheck` 導入（第12）、API 型を `shared/apiTypes.ts` に集約（第13）で計画リファクタリング（P0〜P2）完了。
  - `Docs/` フォルダ新設（設計ドキュメント運用開始）。ポーズ ControlNet フェーズ1（モデル配布 proxy 一般化）。
  - UI 改善: マスク ON 状態の維持、MASK バッジのタグ表示トグル、シード値クリックコピー（`copy-seed`）。
- 2026-07-01 まとめ:
  - 初期整備: 旧仕様書を削除し本ファイルを新設。`AGENTS.md` にテスト DB・ポート・データ境界の規約を整備。パス境界判定を `server/paths.ts` に集約、`generationMode.ts` 共有化（リファクタリング第1〜5フェーズ相当）。
  - マスク編集の基盤実装: WebSAM / SlimSAM-77 スマート選択（ONNX Runtime Worker・OPFS キャッシュ・Point/Box/Brush prompt・GitHub Release proxy）、手動ペン/消しゴムの合成規約、ブラシカーソル、`maskedContent` 既定 `original` 化と paste back マスク分離、MASK 表示の限定。
  - 生成まわり: バッチを1枚単位の `generation_jobs` に分解し WebSocket 監視・逐次取り込み・停止ボタンを実装。3秒 polling。img2img の `ImageScale` リサイズ、denoise 保持。
  - ビューア UX: ホイールズーム・ドラッグ移動、マスクツールバーの最小化・移動・自動最小化、フッター通常フロー化、ショートカットと入力フォーカスの分離、イテレーションツリーのスクロール保持、Workflow diagram（Mermaid）。
